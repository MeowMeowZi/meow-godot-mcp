---
phase: 12-input-injection-enhancement
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - tests/uat_phase12.py
autonomous: false
requirements: [INPT-01, INPT-02, INPT-03]

must_haves:
  truths:
    - "UAT test script covers all three INPT requirements with automated assertions"
    - "Auto-cycle click test verifies auto_cycle:true in response when no explicit pressed param"
    - "Backward-compatible click test verifies pressed field in response when explicit pressed param given"
    - "click_node test verifies success response with clicked_position coordinates"
    - "click_node error test verifies error for non-existent node path"
    - "get_node_rect test verifies position, size, global_position, center, and visible fields"
    - "get_node_rect error test verifies error for non-existent node path"
    - "Tool count test verifies 40 tools including click_node and get_node_rect"
  artifacts:
    - path: "tests/uat_phase12.py"
      provides: "End-to-end UAT suite for Phase 12 input injection enhancements"
      min_lines: 200
  key_links:
    - from: "tests/uat_phase12.py"
      to: "src/mcp_server.cpp"
      via: "TCP JSON-RPC tools/call for click_node, get_node_rect, inject_input"
      pattern: "call_tool.*click_node"
---

<objective>
Create comprehensive UAT test suite for Phase 12 and verify all INPT requirements pass.

Purpose: Automated end-to-end validation of auto-cycle click, click_node, and get_node_rect.
Output: tests/uat_phase12.py -- runnable UAT script covering 10+ test cases.
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-input-injection-enhancement/12-CONTEXT.md
@.planning/phases/12-input-injection-enhancement/12-01-SUMMARY.md

<interfaces>
<!-- Phase 12 tool APIs the UAT must test -->

click_node tool:
- Input: {"node_path": "BackpackUI/BtnSearch"}
- Success response: {"success": true, "clicked_position": {"x": float, "y": float}}
- Error responses: {"error": "Node not found: ..."}, {"error": "Node is not a Control ..."}, {"error": "Node is not visible in tree"}
- Uses deferred response pattern (15s timeout recommended)

get_node_rect tool:
- Input: {"node_path": "BackpackUI/BtnSearch"}
- Success response: {"position": {"x": float, "y": float}, "size": {"width": float, "height": float}, "global_position": {"x": float, "y": float}, "center": {"x": float, "y": float}, "visible": true}
- Error responses: same as click_node
- Uses deferred response pattern (15s timeout recommended)

inject_input auto-cycle click:
- Input: {"type": "mouse", "mouse_action": "click", "position": {"x": 100, "y": 100}} (no pressed param)
- Response: {"success": true, "type": "mouse", "mouse_action": "click", "position": {...}, "button": "left", "auto_cycle": true}

inject_input backward-compatible click:
- Input: {"type": "mouse", "mouse_action": "click", "position": {"x": 100, "y": 100}, "pressed": true}
- Response: {"success": true, ..., "pressed": true} (no auto_cycle field)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UAT test script for Phase 12</name>
  <files>tests/uat_phase12.py</files>
  <read_first>
    - tests/uat_phase10.py
    - .planning/phases/12-input-injection-enhancement/12-RESEARCH.md
  </read_first>
  <action>
Create `tests/uat_phase12.py` following the exact structure of `tests/uat_phase10.py` (MCPClient class, report helper, call_tool/call_tool_text helpers, wait_for_bridge).

**Docstring:** Phase 12 UAT - Automated end-to-end tests for input injection enhancement tools. Lists all INPT requirements.

**Prerequisites comment block:** Same as Phase 10 (Godot editor open, MCP plugin enabled, port 6800, main scene configured).

**Reuse the MCPClient class, PASS/FAIL constants, results list, report(), call_tool(), call_tool_text(), wait_for_bridge() exactly from uat_phase10.py.**

**Tests (in order):**

Test 1: tools/list shows 40 tools including click_node and get_node_rect (INPT-02, INPT-03)
- Call tools/list
- Assert len(tools) == 40
- Assert "click_node" in tool_names and "get_node_rect" in tool_names

Test 2: run_game and bridge connects within 10s
- Call run_game mode=main
- wait_for_bridge(max_wait=10.0)
- Early exit with summary if bridge fails to connect

Test 3: inject_input click auto-cycle (no explicit pressed) (INPT-01)
- Call inject_input with type=mouse, mouse_action=click, position={x:100,y:100}, button=left -- NO pressed param
- Assert success=true, mouse_action=click, auto_cycle=true

Test 4: inject_input click with explicit pressed=true preserves single-fire (INPT-01)
- Call inject_input with type=mouse, mouse_action=click, position={x:100,y:100}, pressed=true
- Assert success=true, pressed=true
- Assert "auto_cycle" NOT in data (the field should not be present)

Test 5: inject_input click with explicit pressed=false preserves single-fire (INPT-01)
- Call inject_input with type=mouse, mouse_action=click, position={x:100,y:100}, pressed=false
- Assert success=true, pressed=false
- Assert "auto_cycle" NOT in data

Test 6: get_node_rect on scene root returns position and size (INPT-03)
- Call get_node_rect with node_path="" (empty string = scene root)
- Use 15s timeout (deferred response)
- Assert response contains "position" with x,y number keys
- Assert response contains "size" with width,height number keys
- Assert response contains "global_position" with x,y number keys
- Assert response contains "center" with x,y number keys
- Assert response contains "visible" == true

Test 7: get_node_rect error for non-existent node (INPT-03)
- Call get_node_rect with node_path="NonExistentNode/DoesNotExist"
- Use 15s timeout
- Assert "error" in response and "not found" in error string (case-insensitive)

Test 8: get_node_rect error for non-visible node (INPT-03)
- NOTE: This test requires a hidden Control node in the scene. If the main scene does not have one, test can be marked as SKIP with detail. However, attempt it with a known path first. If it fails, report with detail explaining the scene may lack a hidden Control.
- Alternative: Skip this test or test with a deliberately hidden node if available

Test 9: click_node on a known Control node (INPT-02)
- First, call get_scene_tree (via the running game -- actually, get_scene_tree queries the EDITOR tree not the game tree). Instead, we need a known node path from the running scene.
- Strategy: Use get_node_rect with node_path="" to confirm the scene root is a Control (most test scenes have Control root). If it IS a Control, call click_node on "". If it is NOT a Control, try a known child.
- Call click_node with the identified node_path, timeout=15s
- Assert success=true
- Assert "clicked_position" in response with x,y numbers

Test 10: click_node returns actual clicked coordinates (INPT-02)
- Use the result from Test 9 (or call click_node again)
- Verify clicked_position.x and clicked_position.y are both numbers >= 0

Test 11: click_node error for non-existent node (INPT-02)
- Call click_node with node_path="NonExistentNode/DoesNotExist"
- timeout=15s
- Assert "error" in response and "not found" in error string (case-insensitive)

Test 12: click_node error for non-Control node (INPT-02)
- NOTE: Requires a non-Control node (like Node2D) in the running scene. If not available, test can attempt a known non-Control path. If fails, report with detail.
- Alternative approach: If scene root is Node2D (not Control), call click_node on "" and verify "not a Control" error.

Test 13: stop_game and bridge disconnects
- Call stop_game
- Sleep 1.0
- Call get_game_bridge_status
- Assert connected=false

**Summary function:** `_print_summary()` with "Phase 12 Input Injection Enhancement UAT Results" header.

**Implementation notes:**
- Use 15s timeout for ALL deferred response tools (click_node, get_node_rect)
- The call_tool_text helper is used for text content results; for click_node and get_node_rect, these return text content with JSON inside
- Handle early exit gracefully (stop game, print partial summary) if bridge fails to connect
- The test order is designed to: (a) verify tools exist, (b) start game, (c) test fire-and-forget inject_input changes first (fast), (d) test deferred tools (slower), (e) test errors, (f) stop game
  </action>
  <verify>
    <automated>cd D:/Workspace/Godot/godot-mcp-meow && python -c "import ast; ast.parse(open('tests/uat_phase12.py').read()); print('Syntax OK')" && grep -c "def test\|report(" tests/uat_phase12.py && grep "auto_cycle\|click_node\|get_node_rect\|INPT-0" tests/uat_phase12.py | head -20</automated>
  </verify>
  <acceptance_criteria>
    - python -c "import ast; ast.parse(open('tests/uat_phase12.py').read())" exits with code 0 (valid Python syntax)
    - grep "auto_cycle" tests/uat_phase12.py returns at least 2 matches (test assertion for auto_cycle=true, and assertion for auto_cycle not in data)
    - grep "click_node" tests/uat_phase12.py returns at least 5 matches (tool name in tests)
    - grep "get_node_rect" tests/uat_phase12.py returns at least 5 matches (tool name in tests)
    - grep "INPT-01" tests/uat_phase12.py returns at least 3 matches (three INPT-01 tests)
    - grep "INPT-02" tests/uat_phase12.py returns at least 3 matches (click_node tests)
    - grep "INPT-03" tests/uat_phase12.py returns at least 2 matches (get_node_rect tests)
    - grep "report(" tests/uat_phase12.py returns at least 10 matches (10+ test cases)
    - grep "timeout=15" tests/uat_phase12.py returns at least 2 matches (deferred tool timeouts)
    - File contains MCPClient class with connect, send, recv, request methods
    - File contains _print_summary function
    - File contains if __name__ == "__main__": run_tests() at bottom
  </acceptance_criteria>
  <done>tests/uat_phase12.py has 13 test cases covering tool count (40), auto-cycle click (INPT-01), backward-compatible click (INPT-01), get_node_rect success and errors (INPT-03), click_node success and errors (INPT-02), and game lifecycle; valid Python syntax</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 12 UAT passes end-to-end</name>
  <files>tests/uat_phase12.py</files>
  <action>
Human verifies the complete Phase 12 implementation by building the project and running the UAT test suite. This checkpoint confirms that auto-cycle click, click_node, and get_node_rect all work end-to-end in a live Godot editor session.
  </action>
  <what-built>Complete Phase 12 implementation: auto-cycle click, click_node tool, get_node_rect tool, and UAT test suite</what-built>
  <how-to-verify>
    1. Build the project: `scons platform=windows target=editor -j8`
    2. Open Godot editor with the project at `project/`
    3. Ensure MCP Meow plugin is enabled
    4. Run UAT: `python tests/uat_phase12.py`
    5. Expected: All 13 tests pass (or 11+ with 1-2 skips for hidden/non-Control node tests)
    6. Verify the following manually in the test output:
       - Test 1 shows 40 tools
       - Test 3 shows auto_cycle: true in response
       - Test 4/5 show pressed field, no auto_cycle
       - Test 6 shows position/size/center/global_position/visible
       - Test 9/10 shows clicked_position with coordinates
  </how-to-verify>
  <verify>python tests/uat_phase12.py exits with code 0</verify>
  <done>All UAT tests pass, confirming INPT-01 (auto-cycle click), INPT-02 (click_node), and INPT-03 (get_node_rect) work correctly in the running game</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Full phase verification:
```bash
python tests/uat_phase12.py
```
Expected: All tests pass. The script exits with code 0 on full pass, code 1 on any failure.
</verification>

<success_criteria>
- UAT script exists at tests/uat_phase12.py with valid Python syntax
- Test coverage: auto-cycle click (3 tests), click_node (4 tests), get_node_rect (3 tests), tool count (1 test), game lifecycle (2 tests)
- All UAT tests pass when run against live Godot editor
- Total tool count confirmed as 40
</success_criteria>

<output>
After completion, create `.planning/phases/12-input-injection-enhancement/12-02-SUMMARY.md`
</output>
