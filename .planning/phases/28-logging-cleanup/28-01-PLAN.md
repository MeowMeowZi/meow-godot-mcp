---
phase: 28-logging-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_server.cpp
  - src/game_bridge.cpp
  - src/error_enrichment.cpp
autonomous: true
requirements:
  - LOG-01
  - CLEAN-01

must_haves:
  truths:
    - "Plugin errors from TCP server failure appear in both Output panel and Debugger > Errors tab"
    - "Plugin errors from game input injection failure appear in both Output panel and Debugger > Errors tab"
    - "TOOL_PARAM_HINTS contains exactly the 30 tools currently registered in mcp_tool_registry.cpp"
    - "No stale tool names from the pre-consolidation 59-tool era remain in TOOL_PARAM_HINTS"
  artifacts:
    - path: "src/mcp_server.cpp"
      provides: "push_error() replacing printerr() at TCP listen failure"
      contains: "push_error"
    - path: "src/game_bridge.cpp"
      provides: "push_error() replacing printerr() at input injection error"
      contains: "push_error"
    - path: "src/error_enrichment.cpp"
      provides: "TOOL_PARAM_HINTS with exactly 30 entries matching current registry"
      contains: "TOOL_PARAM_HINTS"
  key_links:
    - from: "src/mcp_server.cpp"
      to: "Godot Debugger > Errors panel"
      via: "UtilityFunctions::push_error()"
      pattern: "push_error.*Failed to start TCP"
    - from: "src/game_bridge.cpp"
      to: "Godot Debugger > Errors panel"
      via: "UtilityFunctions::push_error()"
      pattern: "push_error.*input injection error"
    - from: "src/error_enrichment.cpp"
      to: "src/mcp_tool_registry.cpp"
      via: "TOOL_PARAM_HINTS keys must be 1:1 subset of registered tool names"
      pattern: "TOOL_PARAM_HINTS"
---

<objective>
Replace printerr() with push_error() for actual error conditions so they appear in both Godot panels, and clean TOOL_PARAM_HINTS to match the current 30-tool registry.

Purpose: LOG-01 ensures developers see plugin errors in the Debugger > Errors tab (not just Output). CLEAN-01 removes 28 stale entries from the 59-tool era and adds 6 missing entries for current tools.
Output: Modified mcp_server.cpp, game_bridge.cpp, error_enrichment.cpp
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/mcp_tool_registry.cpp (tool registry — authoritative list of 30 tools)
@src/error_enrichment.cpp (TOOL_PARAM_HINTS — target for cleanup)
@src/mcp_server.cpp (printerr at line 173)
@src/game_bridge.cpp (printerr at line 348)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace printerr() with push_error() for actual errors (LOG-01)</name>
  <files>src/mcp_server.cpp, src/game_bridge.cpp</files>
  <read_first>src/mcp_server.cpp (lines 168-180), src/game_bridge.cpp (lines 340-355)</read_first>
  <action>
Replace exactly 2 printerr() calls with push_error() so error messages appear in both Output and Debugger > Errors panels (per LOG-01).

**File 1: src/mcp_server.cpp line 173**
Change:
  UtilityFunctions::printerr("MCP Meow: Failed to start TCP server on port ", port, " (error: ", (int)err, ")");
To:
  UtilityFunctions::push_error("MCP Meow: Failed to start TCP server on port ", port, " (error: ", (int)err, ")");

**File 2: src/game_bridge.cpp line 348**
Change:
  UtilityFunctions::printerr("MCP Meow: Game input injection error: ", error_msg);
To:
  UtilityFunctions::push_error("MCP Meow: Game input injection error: ", error_msg);

Do NOT change any UtilityFunctions::print() calls — those are informational messages where Output-only is correct.

Both files already include the necessary godot_cpp headers for push_error (it is in the same UtilityFunctions namespace as printerr).
  </action>
  <verify>
    <automated>cd "D:\Workspace\Godot\godot-mcp-meow" && echo "=== printerr calls (should be 0) ===" && grep -rn "printerr(" src/*.cpp | grep -v "push_error" | grep -v "//"; echo "=== push_error calls (should include our 2 new ones) ===" && grep -rn "push_error(" src/mcp_server.cpp src/game_bridge.cpp</automated>
  </verify>
  <acceptance_criteria>
- grep -rn "printerr(" src/mcp_server.cpp returns 0 matches
- grep -rn "printerr(" src/game_bridge.cpp returns 0 matches
- grep -rn "push_error(" src/mcp_server.cpp returns at least 1 match containing "Failed to start TCP server"
- grep -rn "push_error(" src/game_bridge.cpp returns at least 1 match containing "input injection error"
  </acceptance_criteria>
  <done>Both printerr() calls replaced with push_error(). Zero printerr() calls remain in mcp_server.cpp and game_bridge.cpp. Errors will now appear in both Output and Debugger > Errors panels.</done>
</task>

<task type="auto">
  <name>Task 2: Clean TOOL_PARAM_HINTS to match current 30-tool registry (CLEAN-01)</name>
  <files>src/error_enrichment.cpp</files>
  <read_first>src/error_enrichment.cpp (lines 283-333), src/mcp_tool_registry.cpp (lines 1-30 for tool names)</read_first>
  <action>
Replace the entire TOOL_PARAM_HINTS map in error_enrichment.cpp with a version containing exactly the 30 tools from mcp_tool_registry.cpp. This removes 28 stale entries and adds 6 missing entries (per CLEAN-01).

**Remove these 28 stale entries** (tools that no longer exist after 59-to-30 consolidation):
add_animation_track, click_node, create_animation, create_character, create_collision_shape, create_stylebox, create_ui_panel, detach_script, disconnect_signal, find_nodes, get_animation_info, get_node_rect, get_node_signals, get_resource_info, get_theme_overrides, get_tilemap_cell_info, get_tilemap_info, get_ui_properties, instantiate_scene, run_test_sequence, set_animation_properties, set_container_layout, set_keyframe, set_layout_preset, set_theme_override, bool, float, int

**Keep these 22 entries** (exist in both TOOL_PARAM_HINTS and registry, update text if needed):
attach_script, batch_set_property, capture_game_viewport, capture_viewport, connect_signal, create_node, create_node_tree, create_scene, delete_node, duplicate_node, edit_script, erase_tilemap_cells, eval_in_game, get_game_node_property, inject_input, inject_input_sequence, inject_text, open_scene, read_script, run_game, set_node_property, set_tilemap_cells, validate_scripts, write_script

**Add these 6 missing entries** for tools that exist in registry but had no hint:
- get_scene_tree: "Parameters: max_depth (integer, optional), include_properties (boolean, optional, default true), root_path (string, optional starting node)"
- get_game_output: "Parameters: clear (boolean, optional, clears buffer after read)"
- list_project_files: "Parameters: directory (string, optional, e.g. 'res://scripts'), extensions (array of strings, optional, e.g. ['gd', 'tscn'])"
- save_scene: "Parameters: path (string, optional, e.g. 'res://scenes/main.tscn'). Saves to current path if omitted."
- stop_game: "No parameters required. Stops the currently running game."
- restart_editor: "No parameters required. Restarts the Godot editor."

The final TOOL_PARAM_HINTS map must contain exactly 30 entries — one per registered tool. Sort entries alphabetically for maintainability.
  </action>
  <verify>
    <automated>cd "D:\Workspace\Godot\godot-mcp-meow" && echo "=== TOOL_PARAM_HINTS entry count (should be 30) ===" && grep -c '^\s*{"[a-z_]*",' src/error_enrichment.cpp && echo "=== Stale entries check (should be 0) ===" && for tool in add_animation_track click_node create_animation create_character create_collision_shape create_stylebox create_ui_panel detach_script disconnect_signal find_nodes get_animation_info get_node_rect get_node_signals get_resource_info get_theme_overrides get_tilemap_cell_info get_tilemap_info get_ui_properties instantiate_scene run_test_sequence set_animation_properties set_container_layout set_keyframe set_layout_preset set_theme_override; do grep -q "\"$tool\"" src/error_enrichment.cpp && echo "STALE: $tool"; done; echo "=== New entries check (all 6 should appear) ===" && for tool in get_scene_tree get_game_output list_project_files save_scene stop_game restart_editor; do grep -q "\"$tool\"" src/error_enrichment.cpp && echo "FOUND: $tool" || echo "MISSING: $tool"; done</automated>
  </verify>
  <acceptance_criteria>
- grep -c entry-pattern src/error_enrichment.cpp returns exactly 30
- grep for any of the 28 stale tool names returns 0 matches inside TOOL_PARAM_HINTS
- grep for each of the 6 new tool names (get_scene_tree, get_game_output, list_project_files, save_scene, stop_game, restart_editor) returns 1 match each
- scons build completes without errors
  </acceptance_criteria>
  <done>TOOL_PARAM_HINTS contains exactly 30 entries matching the current mcp_tool_registry.cpp tool set. All 28 stale entries removed. All 6 missing entries added. Map is alphabetically sorted.</done>
</task>

</tasks>

<verification>
1. Zero printerr() calls remain in src/mcp_server.cpp and src/game_bridge.cpp
2. push_error() used for TCP server failure and game input injection error
3. TOOL_PARAM_HINTS has exactly 30 entries
4. No stale tool names from pre-consolidation era in TOOL_PARAM_HINTS
5. All 30 registered tools have a corresponding TOOL_PARAM_HINTS entry
6. Build succeeds: scons target=template_debug -j8
</verification>

<success_criteria>
- LOG-01: Both printerr() calls replaced with push_error(); errors will appear in Output + Debugger > Errors
- CLEAN-01: TOOL_PARAM_HINTS contains exactly 30 entries, 1:1 with mcp_tool_registry.cpp
- Build compiles cleanly with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/28-logging-cleanup/28-01-SUMMARY.md`
</output>
