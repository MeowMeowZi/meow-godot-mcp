---
phase: 14-game-output-enhancement
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - tests/uat_phase14.py
autonomous: false
requirements: [GOUT-01, GOUT-02, GOUT-03]

must_haves:
  truths:
    - "UAT test suite validates get_game_output captures print() output without file_logging"
    - "UAT test suite validates level filtering returns only matching entries"
    - "UAT test suite validates keyword filtering returns only matching entries"
    - "UAT test suite validates clear_after_read incremental behavior"
    - "All UAT tests pass against live Godot editor"
  artifacts:
    - path: "tests/uat_phase14.py"
      provides: "End-to-end UAT suite for all three GOUT requirements"
      min_lines: 300
  key_links:
    - from: "tests/uat_phase14.py"
      to: "src/mcp_server.cpp"
      via: "TCP JSON-RPC call to get_game_output tool"
      pattern: "get_game_output"
---

<objective>
Create UAT test suite and verify all GOUT requirements end-to-end against live Godot editor.

Purpose: Validates that debugger-channel log capture works in practice, structured filtering produces correct results, and the tool count is correct.

Output: UAT test script and human-verified passing results.
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-game-output-enhancement/14-01-SUMMARY.md
@tests/uat_phase13.py

<interfaces>
<!-- Pattern from uat_phase13.py: MCPClient class, call_tool/call_tool_text helpers, run_tests pattern -->
<!-- Tool count after Phase 13: 43 tools. Phase 14 does NOT add new tools, only enhances get_game_output -->
<!-- get_game_output now accepts: clear_after_read, level, since, keyword -->

Enhanced get_game_output response format:
```json
{
  "success": true,
  "lines": [
    {"text": "Hello from game", "level": "info", "timestamp_ms": 123456789},
    {"text": "Warning message", "level": "warning", "timestamp_ms": 123456800}
  ],
  "count": 2,
  "total_buffered": 5,
  "game_running": true
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UAT test script for Phase 14</name>
  <files>tests/uat_phase14.py</files>
  <read_first>
    tests/uat_phase13.py
    src/mcp_tool_registry.cpp
  </read_first>
  <action>
Create `tests/uat_phase14.py` following the exact same structure as `uat_phase13.py` (MCPClient class, call_tool/call_tool_text helpers, run_tests pattern, summary output).

**Test cases (targeting ~12 tests):**

1. **test_tool_count** — tools/list returns 43 tools (no new tools added, get_game_output was existing)
2. **test_get_game_output_schema** — get_game_output appears in tools/list with level, since, keyword properties
3. **test_run_game** — run_game with mode=main succeeds, wait 3s for bridge connection
4. **test_bridge_connected** — get_game_bridge_status shows connected=true. If not connected, early exit with summary.
5. **test_get_game_output_basic** — call get_game_output (no filters). Expect success=true, lines array (may be empty or contain initial print output from scene). Key assertion: response has "lines" array with objects containing "text", "level", "timestamp_ms" fields (when lines are present). If lines is empty, that is OK — we verify the shape on non-empty output later.
6. **test_get_game_output_after_eval** — call eval_in_game with an expression, then call get_game_output with clear_after_read=false to re-read all buffered output. Since the game scene likely has print() calls in _ready(), there should be some output.
7. **test_get_game_output_clear_after_read** — First call with clear_after_read=true to drain. Then call again — should return empty or only new output. Validates incremental read.
8. **test_get_game_output_keyword_filter** — Call with keyword="some_known_string". Assert success=true and that if any lines are returned, they all contain the keyword.
9. **test_get_game_output_level_filter** — Call with level="error". Assert success=true and that if any lines are returned, they all have level="error".
10. **test_get_game_output_no_file_logging_warning** — Verify the earlier run_game result does NOT contain "warning" key about file_logging. This validates GOUT-01/GOUT-03.
11. **test_get_game_output_combined_filters** — Call with level="info" AND keyword="anything". Assert success=true, response has expected shape.
12. **test_stop_game** — stop_game succeeds.

**Important patterns from uat_phase13.py to follow:**
- MCPClient class with connect(), close(), send_request(), call_tool(), call_tool_text()
- 15s timeout for deferred response tools (eval_in_game, etc.)
- 10s default timeout for synchronous tools
- Early exit with summary if bridge fails to connect
- Print PASS/FAIL with test names
- Summary at end with pass/fail counts
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/uat_phase14.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/uat_phase14.py exists with >= 300 lines
    - Contains MCPClient class (same pattern as uat_phase13.py)
    - Contains >= 10 test functions covering GOUT-01, GOUT-02, GOUT-03
    - Tests verify tool count is 43
    - Tests verify get_game_output response shape (lines array with text/level/timestamp_ms)
    - Tests verify keyword and level filtering
    - Tests verify no file_logging warning in run_game response
    - Tests verify clear_after_read incremental behavior
    - Python syntax is valid
  </acceptance_criteria>
  <done>
    UAT script created with >= 10 tests covering all three GOUT requirements, valid Python syntax
  </done>
</task>

<task type="auto">
  <name>Task 2: Build verification</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the SCons build to confirm all Phase 14 changes compile without errors:

```bash
cd D:/Workspace/Godot/godot-mcp-meow && scons platform=windows target=editor dev_build=yes
```

Verify:
1. Build succeeds with zero compilation errors
2. The resulting .dll is produced

If build fails, fix the issue and rebuild.
  </action>
  <verify>
    <automated>cd D:/Workspace/Godot/godot-mcp-meow && scons platform=windows target=editor dev_build=yes 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - scons build completes with zero errors
    - Output .dll exists in project/addons/meow_godot_mcp/bin/
  </acceptance_criteria>
  <done>
    Build passes, plugin binary is updated with Phase 14 changes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — run UAT against live Godot editor</name>
  <files></files>
  <action>
    Human runs UAT test suite against live Godot editor to verify all GOUT requirements work end-to-end.
    Claude has already automated all implementation and testing. This checkpoint verifies the integration works in practice.
  </action>
  <verify>
    <automated>python tests/uat_phase14.py 2>&1 | tail -20</automated>
  </verify>
  <done>
    Human confirms all UAT tests pass, print() output captured without file_logging, structured filtering works
  </done>
  <what-built>
    Phase 14 Game Output Enhancement:
    - get_game_output now captures print() output via EditorDebugger channel (no file_logging needed)
    - Supports structured filtering: level (info/warning/error), since (timestamp), keyword (substring)
    - Backward compatible file-based fallback when bridge unavailable
    - UAT test suite with 12 tests covering all GOUT requirements
  </what-built>
  <how-to-verify>
    1. Open the Godot project at `project/` with the MCP Meow plugin enabled
    2. Ensure the MCP TCP server is listening on port 6800 (check Output panel)
    3. Run: `python tests/uat_phase14.py`
    4. Expected: All tests pass (PASS on each line, summary shows all green)
    5. Key things to check:
       - run_game response does NOT have file_logging warning
       - get_game_output returns lines with text/level/timestamp_ms structure
       - Filtering tests pass (level, keyword)
       - clear_after_read incremental behavior works
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `tests/uat_phase14.py` exists and has valid Python syntax
2. Build succeeds: `scons platform=windows target=editor dev_build=yes`
3. All UAT tests pass against live Godot editor
4. Human confirms output capture works without file_logging setting
</verification>

<success_criteria>
- UAT test suite passes all tests
- print() output captured automatically via debugger channel
- No file_logging warning in run_game response
- Structured filtering (level, keyword) returns correct results
- Human approves Phase 14 completion
</success_criteria>

<output>
After completion, create `.planning/phases/14-game-output-enhancement/14-02-SUMMARY.md`
</output>
