---
phase: 04-editor-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_dock.h
  - src/mcp_dock.cpp
  - src/mcp_tool_registry.h
  - src/mcp_tool_registry.cpp
  - src/mcp_plugin.h
  - src/mcp_plugin.cpp
  - src/mcp_server.h
  - src/mcp_server.cpp
  - src/mcp_protocol.h
  - src/mcp_protocol.cpp
  - tests/test_tool_registry.cpp
  - tests/test_protocol.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03]

must_haves:
  truths:
    - "Dock panel appears in Godot editor right side with tab titled 'MCP Meow'"
    - "Dock displays three-state status: Stopped (server off), Waiting (server on, no client), Connected (client connected)"
    - "Dock displays listening port, detected Godot version, and available tool count"
    - "Toggle button switches between Start/Stop text and starts/stops MCPServer"
    - "Restart button stops then starts the server"
    - "Status display updates automatically every ~1 second via polling"
    - "ToolDef registry contains all 12 existing tools with min_version {4,3,0}"
    - "create_tools_list_response filters tools by detected Godot version"
  artifacts:
    - path: "src/mcp_dock.h"
      provides: "MCPDock class declaration"
      contains: "class MCPDock"
    - path: "src/mcp_dock.cpp"
      provides: "Dock UI construction and status update logic"
      min_lines: 50
    - path: "src/mcp_tool_registry.h"
      provides: "GodotVersion struct, ToolDef struct, tool registry API"
      exports: ["GodotVersion", "ToolDef", "get_all_tools", "get_filtered_tools_json"]
    - path: "src/mcp_tool_registry.cpp"
      provides: "All 12 tool definitions with schemas and version gates"
      min_lines: 100
    - path: "src/mcp_plugin.h"
      provides: "MCPDock pointer, button callbacks, status timer, version fields"
      contains: "MCPDock"
    - path: "src/mcp_plugin.cpp"
      provides: "Dock lifecycle, signal wiring, status polling, version detection"
      min_lines: 60
    - path: "src/mcp_server.h"
      provides: "client_connected atomic flag and has_client() getter"
      contains: "client_connected"
    - path: "tests/test_tool_registry.cpp"
      provides: "Unit tests for version comparison and tool filtering"
      min_lines: 40
  key_links:
    - from: "src/mcp_plugin.cpp"
      to: "src/mcp_dock.h"
      via: "MCPPlugin creates/destroys MCPDock in _enter_tree/_exit_tree"
      pattern: "new MCPDock"
    - from: "src/mcp_plugin.cpp"
      to: "src/mcp_server.h"
      via: "MCPPlugin queries server->has_client() for dock status"
      pattern: "has_client"
    - from: "src/mcp_plugin.cpp"
      to: "dock button signals"
      via: "callable_mp(this, &MCPPlugin::_on_toggle_pressed)"
      pattern: "callable_mp"
    - from: "src/mcp_protocol.cpp"
      to: "src/mcp_tool_registry.h"
      via: "create_tools_list_response calls get_filtered_tools_json"
      pattern: "get_filtered_tools_json"
---

<objective>
Dock panel UI with connection status, server controls, and version-aware tool registry

Purpose: Deliver the three core editor integration features -- a visible dock panel showing MCP server state (EDIT-01), buttons to control the server lifecycle (EDIT-02), and a ToolDef registry that can filter tools by Godot version (EDIT-03). This gives users real-time visibility and control over the MCP server without reloading the plugin.

Output: MCPDock class (UI), ToolDef registry module, MCPPlugin integration with button signals and status polling, MCPServer client_connected flag, updated protocol layer using registry, unit tests for tool registry and version filtering
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-editor-integration/04-CONTEXT.md
@.planning/phases/04-editor-integration/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/mcp_plugin.h (current):
```cpp
class MCPPlugin : public godot::EditorPlugin {
    GDCLASS(MCPPlugin, godot::EditorPlugin);
public:
    void _enter_tree() override;
    void _exit_tree() override;
    void _process(double delta) override;
protected:
    static void _bind_methods();
private:
    MCPServer* server;
    int port;
};
```

From src/mcp_server.h (current):
```cpp
class MCPServer {
public:
    void start(int port = 6800);
    void stop();
    void poll();
    bool is_running() const;
    void set_undo_redo(godot::EditorUndoRedoManager* ur);
private:
    std::atomic<bool> running{false};
    godot::Ref<godot::StreamPeerTCP> client_peer;
    // ... threading members
};
```

From src/mcp_protocol.h (current):
```cpp
namespace mcp {
    nlohmann::json create_initialize_response(const nlohmann::json& id);
    nlohmann::json create_tools_list_response(const nlohmann::json& id);
    nlohmann::json create_tool_result(const nlohmann::json& id, const nlohmann::json& content_data);
    nlohmann::json create_error_response(const nlohmann::json& id, int code, const std::string& message);
    nlohmann::json create_tool_not_found_error(const nlohmann::json& id, const std::string& tool_name);
    nlohmann::json create_resources_list_response(const nlohmann::json& id, const nlohmann::json& resources);
    nlohmann::json create_resource_read_response(const nlohmann::json& id, const nlohmann::json& contents);
}
```

From src/mcp_server.cpp io_thread_func (client connect/disconnect points):
```cpp
// Line 84: client_peer = tcp_server->take_connection();
// Line 96-99: client disconnect path: client_peer.unref();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToolDef registry with version filtering + MCPServer client_connected flag</name>
  <files>src/mcp_tool_registry.h, src/mcp_tool_registry.cpp, src/mcp_server.h, src/mcp_server.cpp, src/mcp_protocol.h, src/mcp_protocol.cpp, tests/test_tool_registry.cpp, tests/test_protocol.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - GodotVersion comparison: {4,3,0} >= {4,3,0} is true, {4,2,0} >= {4,3,0} is false, {4,4,0} >= {4,3,0} is true, {5,0,0} >= {4,3,0} is true
    - get_all_tools() returns exactly 12 ToolDef entries (all current tools), each with min_version {4,3,0}
    - get_filtered_tools_json({4,3,0}) returns JSON array with 12 tools matching current create_tools_list_response format
    - get_filtered_tools_json({4,2,0}) returns 0 tools (all tools require 4.3+)
    - get_tool_count({4,3,0}) returns 12
    - create_tools_list_response(id) backward-compatible overload still works (uses permissive version {99,99,99})
    - create_tools_list_response(id, version) new overload filters by version
    - Existing test_protocol.cpp tests for tools/list still pass unchanged (backward compat)
    - MCPServer::has_client() returns bool (thread-safe atomic read)
  </behavior>
  <action>
    **Step 1: Create src/mcp_tool_registry.h**
    Define GodotVersion struct with major/minor/patch int fields and operator>= for comparison. Define ToolDef struct with std::string name, std::string description, nlohmann::json input_schema, GodotVersion min_version. Declare functions:
    - `const std::vector<ToolDef>& get_all_tools()` -- returns static registry of all tools
    - `nlohmann::json get_filtered_tools_json(const GodotVersion& current)` -- returns JSON array of tools where current >= tool.min_version
    - `int get_tool_count(const GodotVersion& current)` -- count of available tools for version

    This file is pure C++17 + nlohmann/json (NO Godot headers) for testability.

    **Step 2: Create src/mcp_tool_registry.cpp**
    Implement get_all_tools() with a static std::vector<ToolDef> containing all 12 existing tools. Copy the exact schema JSON from current `create_tools_list_response()` in mcp_protocol.cpp for each tool. All tools get min_version {4,3,0}. The 12 tools are: get_scene_tree, create_node, set_node_property, delete_node, read_script, write_script, edit_script, attach_script, detach_script, list_project_files, get_project_settings, get_resource_info.

    Implement get_filtered_tools_json() -- iterate all tools, check current >= min_version, build JSON array with name/description/inputSchema for each passing tool.

    Implement get_tool_count() -- count tools passing version check.

    **Step 3: Refactor src/mcp_protocol.h and src/mcp_protocol.cpp**
    Add new overload: `nlohmann::json create_tools_list_response(const nlohmann::json& id, const GodotVersion& version)`. Include mcp_tool_registry.h. The new overload calls get_filtered_tools_json(version) and wraps in standard JSON-RPC response. The OLD `create_tools_list_response(const nlohmann::json& id)` overload remains but internally calls the new overload with GodotVersion{99,99,99} for backward compatibility. This is critical -- Pitfall 5 from RESEARCH.md warns that breaking existing tests is a risk. The old function body (giant inline JSON) is replaced by the delegation call.

    Forward-declare GodotVersion in mcp_protocol.h (or include mcp_tool_registry.h).

    **Step 4: Add client_connected to MCPServer**
    In src/mcp_server.h: add `std::atomic<bool> client_connected{false}` member. Add public method `bool has_client() const`.
    In src/mcp_server.cpp:
    - In io_thread_func(), after `client_peer = tcp_server->take_connection()` (line ~84): add `client_connected.store(true)`
    - In io_thread_func(), after `client_peer.unref()` in disconnect path (line ~98): add `client_connected.store(false)`
    - In stop(): add `client_connected.store(false)` in the cleanup section
    - Implement has_client(): `return client_connected.load();`

    **Step 5: Create tests/test_tool_registry.cpp**
    GoogleTest file testing:
    - GodotVersion comparison operators (equal, less, greater, major-differs, minor-differs, patch-differs)
    - get_all_tools() returns 12 tools, each has non-empty name/description/input_schema
    - get_filtered_tools_json({4,3,0}) returns 12 tools
    - get_filtered_tools_json({4,2,0}) returns 0 tools
    - get_filtered_tools_json({99,99,99}) returns 12 tools
    - get_tool_count matches filtered array size
    - Each ToolDef has valid inputSchema with "type":"object"

    **Step 6: Update tests/CMakeLists.txt**
    Add test_tool_registry executable linking mcp_tool_registry.cpp and GTest::gtest_main. Add gtest_discover_tests for it.

    **Step 7: Update tests/test_protocol.cpp**
    Update the HasGetSceneTreeTool test: `ASSERT_EQ(tools.size(), 12)` -- this should still pass since backward-compatible overload returns all tools. No changes needed to existing tests if backward compatibility is correct. Verify by running full test suite.

    **Step 8: Build and verify tests**
    Rebuild test suite: `cd tests/build && cmake .. && cmake --build . --config Release && ctest --output-on-failure`
  </action>
  <verify>
    <automated>cd D:/Workspace/Godot/godot-mcp-meow/tests/build && cmake .. && cmake --build . --config Release && ctest --output-on-failure</automated>
  </verify>
  <done>
    - GodotVersion comparison works correctly for all edge cases
    - ToolDef registry contains all 12 tools with correct schemas
    - Version filtering returns correct subsets
    - create_tools_list_response backward-compatible overload passes all existing tests
    - MCPServer has thread-safe has_client() method
    - All existing tests plus new test_tool_registry tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: MCPDock UI + MCPPlugin integration with dock lifecycle, buttons, and status polling</name>
  <files>src/mcp_dock.h, src/mcp_dock.cpp, src/mcp_plugin.h, src/mcp_plugin.cpp</files>
  <action>
    **Step 1: Create src/mcp_dock.h**
    ```
    #ifndef GODOT_MCP_MEOW_MCP_DOCK_H
    #define GODOT_MCP_MEOW_MCP_DOCK_H
    ```
    Include godot_cpp headers: VBoxContainer, HBoxContainer, Label, Button, HSeparator.

    Declare MCPDock as a plain C++ class (NOT a Godot Object, NOT ClassDB registered -- per CONTEXT.md locked decision). Members:
    - `godot::VBoxContainer* root` -- root container, its name becomes dock tab title
    - `godot::Label* status_label` -- shows "Status: Connected/Waiting/Stopped"
    - `godot::Label* port_label` -- shows "Port: 6800"
    - `godot::Label* version_label` -- shows "Godot: 4.X.Y"
    - `godot::Label* tools_label` -- shows "Tools: 12"
    - `godot::Button* toggle_button` -- Start/Stop toggle
    - `godot::Button* restart_button` -- Restart
    - Cached state for dirty checking: `bool last_running`, `bool last_connected`, `int last_tool_count`

    Public methods:
    - Constructor: builds the VBoxContainer tree with memnew()
    - `godot::VBoxContainer* get_root() const` -- for add_control_to_dock
    - `godot::Button* get_toggle_button() const` -- for signal connection
    - `godot::Button* get_restart_button() const` -- for signal connection
    - `void update_status(bool running, bool client_connected, int port, const std::string& version, int tool_count)` -- updates labels, only if state changed (dirty check)
    - `void update_buttons(bool running)` -- updates button text (Start vs Stop) and restart button disabled state

    **Step 2: Create src/mcp_dock.cpp**
    Constructor:
    - `root = memnew(godot::VBoxContainer)` then `root->set_name("MCP Meow")` (dock tab title -- per Pitfall 3)
    - Create status_label with initial text "Status: Stopped", add to root
    - Create port_label with initial text "Port: --", add to root
    - Create version_label with initial text "Godot: detecting...", add to root
    - Create tools_label with initial text "Tools: 0", add to root
    - Create HSeparator, add to root
    - Create HBoxContainer for buttons, add to root
    - Create toggle_button with text "Start", set `set_h_size_flags(godot::Control::SIZE_EXPAND_FILL)`, add to button container
    - Create restart_button with text "Restart", set `set_h_size_flags(godot::Control::SIZE_EXPAND_FILL)`, add to button container
    - Initialize cached state: last_running = false, last_connected = false, last_tool_count = 0

    update_status(): Compare running/client_connected/tool_count with cached values. If any changed:
    - If !running: status_label->set_text("Status: Stopped")
    - Else if running && !client_connected: status_label->set_text("Status: Waiting for client...")
    - Else: status_label->set_text("Status: Connected")
    - port_label->set_text("Port: " + std::to_string(port)) or "Port: --" if !running
    - version_label->set_text("Godot: " + version)
    - tools_label->set_text("Tools: " + std::to_string(tool_count))
    - Update cached state

    update_buttons(): If running: toggle_button text = "Stop", restart enabled. If !running: toggle_button text = "Start", restart disabled.

    Destructor: empty (Godot owns the nodes via scene tree -- memdelete happens in MCPPlugin::_exit_tree)

    **Step 3: Update src/mcp_plugin.h**
    Forward-declare MCPDock. Add #include for callable_method_pointer and mcp_tool_registry (for GodotVersion).
    Add private members:
    - `MCPDock* dock` -- raw pointer, same ownership pattern as server
    - `double status_timer` -- delta accumulator for polling
    - `GodotVersion detected_version` -- detected at startup
    - `std::string version_string` -- cached "4.X.Y" display string
    - `int tool_count` -- cached available tool count

    Add private methods:
    - `void _on_toggle_pressed()` -- button callback
    - `void _on_restart_pressed()` -- button callback

    **Step 4: Update src/mcp_plugin.cpp**
    Add includes: mcp_dock.h, mcp_tool_registry.h, engine.hpp, callable_method_pointer.hpp

    In constructor: initialize dock = nullptr, status_timer = 0.0, detected_version = {4,3,0}

    In _enter_tree():
    - Detect version via Engine::get_singleton()->get_version_info() (see RESEARCH.md code example). Cast Dictionary values to int for major/minor/patch. Store in detected_version. Format version_string as "major.minor.patch".
    - Compute tool_count = get_tool_count(detected_version)
    - Create server, set_undo_redo, start (existing code)
    - Create dock: `dock = new MCPDock()`
    - Connect button signals TO MCPPlugin (NOT to dock -- per CONTEXT.md locked decision and Pitfall 1):
      ```
      dock->get_toggle_button()->connect("pressed", callable_mp(this, &MCPPlugin::_on_toggle_pressed));
      dock->get_restart_button()->connect("pressed", callable_mp(this, &MCPPlugin::_on_restart_pressed));
      ```
    - Add dock to editor: `add_control_to_dock(DOCK_SLOT_RIGHT_BL, dock->get_root())` (per CONTEXT.md locked decision, 4.3+ compatible)
    - set_process(true)
    - Print startup message including version

    In _exit_tree() (order matters -- per Pitfall 2):
    - If dock: `remove_control_from_docks(dock->get_root())`, then `memdelete(dock->get_root())`, then `delete dock; dock = nullptr;`
    - If server: stop(), delete, nullptr (existing code)

    In _process():
    - Existing poll logic for server (unchanged)
    - Status polling timer: accumulate delta, check every 1.0 second (per CONTEXT.md: 0.5-1s, using 1.0):
      ```
      status_timer += delta;
      if (status_timer >= 1.0 && dock) {
          status_timer = 0.0;
          bool running = server && server->is_running();
          bool connected = server && server->has_client();
          int current_port = running ? port : 0;
          dock->update_status(running, connected, current_port, version_string, tool_count);
          dock->update_buttons(running);
      }
      ```

    Implement _on_toggle_pressed():
    - If server->is_running(): server->stop()
    - Else: server->start(port)
    - Immediately update dock buttons and status

    Implement _on_restart_pressed():
    - server->stop(), server->start(port)
    - Immediately update dock buttons and status

    **Step 5: Update handle_request in mcp_server.cpp**
    Change `tools/list` handler to pass detected version. The MCPServer needs access to the detected version. Two options:
    - Option A: MCPServer stores the version (set from MCPPlugin at startup)
    - Option B: Pass version from handle_request to create_tools_list_response

    Use Option A: Add `GodotVersion godot_version{4,3,0}` member to MCPServer and a `void set_godot_version(const GodotVersion& v)` setter. MCPPlugin calls `server->set_godot_version(detected_version)` after detection in _enter_tree. In handle_request, tools/list calls `mcp::create_tools_list_response(id, godot_version)`.

    **Step 6: Build GDExtension**
    Run `scons` to verify the full GDExtension builds. The new .cpp files (mcp_dock.cpp, mcp_tool_registry.cpp) will be picked up automatically by `Glob("src/*.cpp")` in SConstruct.

    **Step 7: Verify full test suite still passes**
    Rebuild and run unit tests to ensure refactored protocol layer is backward compatible.
  </action>
  <verify>
    <automated>cd D:/Workspace/Godot/godot-mcp-meow && scons -j8 2>&1 | tail -5 && cd tests/build && cmake --build . --config Release && ctest --output-on-failure</automated>
  </verify>
  <done>
    - mcp_dock.h/cpp compile and build as part of GDExtension
    - MCPPlugin creates dock in _enter_tree, removes in _exit_tree
    - Button signals connected via callable_mp to MCPPlugin callbacks
    - Status polling runs every 1s with dirty check
    - Version detection reads Engine::get_version_info() at startup
    - MCPServer exposes has_client() via atomic bool
    - MCPServer stores detected Godot version, passes to tools/list response
    - GDExtension builds without errors
    - All existing unit tests pass
    - Dock panel UI visible when plugin loads in Godot editor (manual verify in Plan 04-02 UAT)
  </done>
</task>

</tasks>

<verification>
1. Unit tests: `cd tests/build && cmake .. && cmake --build . --config Release && ctest --output-on-failure` -- all tests pass including new test_tool_registry
2. GDExtension build: `scons -j8` completes without errors
3. New files exist: mcp_dock.h/cpp, mcp_tool_registry.h/cpp in src/
4. MCPServer has has_client() method with atomic backing
5. create_tools_list_response backward compatibility verified by existing test suite
</verification>

<success_criteria>
- GDExtension compiles with 4 new source files (mcp_dock.h/cpp, mcp_tool_registry.h/cpp)
- All 6 existing test executables pass plus new test_tool_registry
- ToolDef registry contains all 12 tools, version filtering works
- MCPPlugin has dock lifecycle, button callbacks, and status polling
- MCPServer has thread-safe client connection state
</success_criteria>

<output>
After completion, create `.planning/phases/04-editor-integration/04-01-SUMMARY.md`
</output>
