---
phase: 10-running-game-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_tool_registry.cpp
  - tests/test_tool_registry.cpp
autonomous: true
requirements: [BRDG-01, BRDG-02, BRDG-03, BRDG-04, BRDG-05]

must_haves:
  truths:
    - "tools/list returns 38 tools (35 existing + 3 new game bridge tools)"
    - "inject_input tool has type enum (key, mouse, action) with per-type params"
    - "capture_game_viewport tool is registered with optional width/height params"
    - "get_game_bridge_status tool is registered with no required params"
  artifacts:
    - path: "src/mcp_tool_registry.cpp"
      provides: "3 new ToolDef entries for game bridge phase"
      contains: "inject_input"
    - path: "tests/test_tool_registry.cpp"
      provides: "Updated unit tests for 38 tools with schema validation"
      contains: "HasExactly38Tools"
  key_links:
    - from: "src/mcp_tool_registry.cpp"
      to: "tests/test_tool_registry.cpp"
      via: "get_all_tools() returns 38 entries"
      pattern: "tools.size\\(\\).*38"
---

<objective>
Register 3 new game bridge MCP tool definitions (inject_input, capture_game_viewport, get_game_bridge_status) in the tool registry and update all unit tests from 35 to 38 tools.

Purpose: Establishes the tool contracts (schemas, parameter types, required fields) that Plan 02 will implement. All tool names and schemas are finalized here so tests can validate structure before implementation.

Output: Updated mcp_tool_registry.cpp with 38 tools, updated test_tool_registry.cpp with 38-tool assertions and schema validation for all 3 new tools.
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-running-game-bridge/10-CONTEXT.md
@.planning/phases/10-running-game-bridge/10-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from existing code -->

From src/mcp_tool_registry.h:
```cpp
struct GodotVersion {
    int major; int minor; int patch;
    bool operator>=(const GodotVersion& other) const;
    bool operator==(const GodotVersion& other) const;
};

struct ToolDef {
    std::string name;
    std::string description;
    nlohmann::json input_schema;
    GodotVersion min_version;
};

const std::vector<ToolDef>& get_all_tools();
nlohmann::json get_filtered_tools_json(const GodotVersion& current);
int get_tool_count(const GodotVersion& current);
```

Current registry: 35 tools total, all min_version {4,3,0}. Last tool is "capture_viewport" (Phase 9). Pattern: ToolDef entries in static vector inside get_all_tools(), grouped by phase with comments.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 3 game bridge ToolDefs in mcp_tool_registry.cpp</name>
  <files>src/mcp_tool_registry.cpp</files>
  <read_first>src/mcp_tool_registry.cpp, src/mcp_tool_registry.h, .planning/phases/10-running-game-bridge/10-CONTEXT.md</read_first>
  <action>
Add 3 new ToolDef entries at the end of the tools vector (after capture_viewport, before the closing brace), with a Phase 10 comment header. All use min_version {4,3,0}.

**Tool 1: inject_input**
- name: "inject_input"
- description: "Inject input events into a running game. Supports keyboard keys, mouse events, and input actions. Requires a game to be running with the MCP bridge connected."
- inputSchema:
  - type: "object"
  - properties:
    - type: string, enum ["key", "mouse", "action"], description "Input type to inject"
    - keycode: string, description "Key name for type=key (e.g. 'W', 'space', 'escape', 'enter'). Uses Godot key names."
    - pressed: boolean, description "Whether the key/action is pressed (true) or released (false). Default: true"
    - action_name: string, description "Action map name for type=action (e.g. 'ui_accept', 'move_left')"
    - mouse_action: string, enum ["move", "click", "scroll"], description "Mouse action for type=mouse"
    - position: object with properties x (number) and y (number), description "Mouse position in viewport coordinates for type=mouse"
    - button: string, enum ["left", "right", "middle"], description "Mouse button for type=mouse click. Default: left"
    - direction: string, enum ["up", "down"], description "Scroll direction for type=mouse scroll"
  - required: ["type"]

**Tool 2: capture_game_viewport**
- name: "capture_game_viewport"
- description: "Capture a screenshot of the running game's viewport. Returns the image as base64-encoded PNG via MCP ImageContent. Requires a game to be running with the MCP bridge connected."
- inputSchema:
  - type: "object"
  - properties:
    - width: integer, description "Optional output width in pixels. Omit for original game resolution."
    - height: integer, description "Optional output height in pixels. Omit for original game resolution."
  - required: [] (empty array)

**Tool 3: get_game_bridge_status**
- name: "get_game_bridge_status"
- description: "Query the connection status of the game bridge. Returns whether a game is running, bridge connected, and session info."
- inputSchema:
  - type: "object"
  - properties: {} (empty object)
  - required: [] (empty array)
  </action>
  <verify>
    <automated>cd D:/Workspace/Godot/godot-mcp-meow/tests/build && cmake --build . --config Release 2>&1 | tail -5 && ctest -R test_tool_registry --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "inject_input" src/mcp_tool_registry.cpp returns at least 1
    - grep -c "capture_game_viewport" src/mcp_tool_registry.cpp returns at least 1
    - grep -c "get_game_bridge_status" src/mcp_tool_registry.cpp returns at least 1
    - grep "Phase 10" src/mcp_tool_registry.cpp returns the comment header
  </acceptance_criteria>
  <done>3 new ToolDef entries added to get_all_tools() static vector with Phase 10 comment, total 38 tools</done>
</task>

<task type="auto">
  <name>Task 2: Update unit tests for 38 tools with game bridge schema validation</name>
  <files>tests/test_tool_registry.cpp</files>
  <read_first>tests/test_tool_registry.cpp, src/mcp_tool_registry.cpp</read_first>
  <action>
Update ALL existing count assertions from 35 to 38 in these tests:
- HasExactly35Tools -> HasExactly38Tools: change ASSERT_EQ(tools.size(), 35) to 38
- Version430Returns35Tools -> Version430Returns38Tools: change EXPECT_EQ(json_tools.size(), 35) to 38
- PermissiveVersionReturns35Tools -> PermissiveVersionReturns38Tools: change EXPECT_EQ(json_tools.size(), 35) to 38
- Version430Returns35 (ToolCount) -> Version430Returns38: change EXPECT_EQ(get_tool_count(...), 35) to 38

Update ToolNamesAreCorrect: add "inject_input", "capture_game_viewport", "get_game_bridge_status" to the expected_names vector (after "capture_viewport").

Add 3 new schema validation tests in a "Phase 10 Game Bridge" section:

**InjectInputSchemaValidation:**
- properties contains: type, keycode, pressed, action_name, mouse_action, position, button, direction
- required size is 1, required[0] is "type"
- type property has enum with 3 values: "key", "mouse", "action"
- mouse_action has enum: "move", "click", "scroll"
- button has enum: "left", "right", "middle"
- direction has enum: "up", "down"

**CaptureGameViewportSchemaValidation:**
- properties contains: width, height
- width type is "integer", height type is "integer"
- required is empty

**GetGameBridgeStatusSchemaValidation:**
- required is empty
  </action>
  <verify>
    <automated>cd D:/Workspace/Godot/godot-mcp-meow/tests/build && cmake --build . --config Release 2>&1 | tail -5 && ctest -R test_tool_registry --output-on-failure 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "38" tests/test_tool_registry.cpp returns multiple matches (count assertions)
    - grep "inject_input" tests/test_tool_registry.cpp returns matches
    - grep "capture_game_viewport" tests/test_tool_registry.cpp returns matches
    - grep "get_game_bridge_status" tests/test_tool_registry.cpp returns matches
    - grep "InjectInputSchemaValidation" tests/test_tool_registry.cpp returns a match
    - All tests pass: ctest output shows 0 failures
  </acceptance_criteria>
  <done>All tool registry tests pass with 38-tool counts, 3 new schema validation tests verify inject_input enum constraints, capture_game_viewport optional params, and get_game_bridge_status empty schema</done>
</task>

</tasks>

<verification>
cd D:/Workspace/Godot/godot-mcp-meow/tests/build && cmake --build . --config Release && ctest --output-on-failure
All tests pass with 38-tool assertions.
</verification>

<success_criteria>
- mcp_tool_registry.cpp contains 38 ToolDef entries (35 existing + 3 new)
- test_tool_registry.cpp updated: all count assertions say 38, expected_names includes 3 new tools
- 3 new schema validation tests exist and pass
- Full test suite passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/10-running-game-bridge/10-01-SUMMARY.md`
</output>
