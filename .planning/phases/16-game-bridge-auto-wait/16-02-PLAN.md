---
phase: 16-game-bridge-auto-wait
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - tests/uat_phase16.py
autonomous: false
requirements: [DX-01, DX-02]

must_haves:
  truths:
    - "run_game with wait_for_bridge=true returns bridge_connected=true when game connects"
    - "run_game with wait_for_bridge=true returns timeout error when timeout exceeded"
    - "set_node_property with node_path='' operates on scene root"
    - "set_node_property with node_path='.' operates on scene root"
    - "All node_path tools accept empty string without error"
  artifacts:
    - path: "tests/uat_phase16.py"
      provides: "End-to-end UAT suite for DX-01 and DX-02"
      contains: "uat_phase16"
  key_links:
    - from: "tests/uat_phase16.py"
      to: "MCP TCP server on port 6800"
      via: "JSON-RPC over TCP"
      pattern: "call_tool"
---

<objective>
Create UAT test suite validating DX-01 (wait_for_bridge) and DX-02 (node_path root) end-to-end.

Purpose: Verify the Phase 16 improvements work correctly against a live Godot editor.
Output: tests/uat_phase16.py with comprehensive test coverage.
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-game-bridge-auto-wait/16-01-SUMMARY.md

<interfaces>
<!-- UAT pattern from prior phases -->
From tests/uat_phase15.py:
```python
class MCPClient:
    def connect(self): ...
    def request(self, method, params=None, timeout=None): ...
    def notify(self, method, params=None): ...

def call_tool(client, tool_name, args=None, timeout=10):
    resp = client.request("tools/call", {"name": tool_name, "arguments": args or {}}, timeout=timeout)
    # returns parsed result text as dict

def call_tool_text(client, tool_name, args=None, timeout=10):
    # returns raw text string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UAT test suite for Phase 16</name>
  <files>tests/uat_phase16.py</files>
  <read_first>tests/uat_phase15.py, src/mcp_tool_registry.cpp</read_first>
  <action>
Create tests/uat_phase16.py following exact uat_phase15.py structure (MCPClient class, helper functions, test runner).

Test cases to implement:

**DX-01 tests (wait_for_bridge):**
1. `test_run_game_schema_has_wait_for_bridge` -- tools/list, find run_game, verify schema has wait_for_bridge and timeout properties
2. `test_run_game_wait_for_bridge` -- call run_game with mode="main", wait_for_bridge=true, timeout=15000. Verify response has success=true, bridge_connected=true. Use 20s socket timeout.
3. `test_bridge_connected_after_wait` -- call get_game_bridge_status, verify connected=true (bridge should be connected from previous test)
4. `test_run_game_already_running_with_wait` -- call run_game again with wait_for_bridge=true while game is running. Should return already_running=true with bridge_connected=true immediately.
5. `test_stop_game` -- stop_game to clean up

**DX-02 tests (node_path root):**
These tests run against the editor scene (no game needed):
6. `test_set_node_property_empty_path` -- call set_node_property with node_path="", property="name", value=current root name. Should succeed (not error about missing parameter).
7. `test_set_node_property_dot_path` -- call set_node_property with node_path=".", property="name", value=current root name. Should succeed.
8. `test_get_node_signals_empty_path` -- call get_node_signals with node_path="". Should return signals list (not error).
9. `test_get_node_signals_dot_path` -- call get_node_signals with node_path=".". Should return signals list.
10. `test_get_ui_properties_empty_path` -- call get_ui_properties with node_path="" on a scene with Control root. May error if root is not Control, but should NOT error about "missing parameter".
11. `test_get_theme_overrides_empty_path` -- call get_theme_overrides with node_path="". Same logic.
12. `test_click_node_empty_path_no_game` -- call click_node with node_path="". Should error about "bridge not connected" (not "missing parameter").
13. `test_get_node_rect_empty_path_no_game` -- call get_node_rect with node_path="". Should error about "bridge not connected" (not "missing parameter").

Order: Run DX-02 tests first (no game needed), then DX-01 tests (need game launch), then stop_game.

Use early exit pattern: if bridge fails to connect during DX-01 tests, print summary and exit without cascading failures.

Use 15s timeout for deferred response tools (wait_for_bridge).
  </action>
  <verify>
    <automated>python tests/uat_phase16.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File tests/uat_phase16.py exists and is valid Python
    - grep -c "def test_" tests/uat_phase16.py returns >= 10
    - grep -c "wait_for_bridge" tests/uat_phase16.py returns >= 3
    - grep -c "has_node_path\|empty.*path\|node_path.*root" tests/uat_phase16.py returns >= 3
  </acceptance_criteria>
  <done>UAT suite created with 13+ tests covering both DX-01 and DX-02. Tests pass when run against live Godot editor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 16 UAT test suite validating wait_for_bridge and node_path root handling</what-built>
  <how-to-verify>
    1. Ensure Godot editor is open with the project at project/ and MCP plugin enabled
    2. Ensure a main scene is configured (project/test_root.tscn)
    3. Run: python tests/uat_phase16.py
    4. Verify output shows all tests passing (13/13 or similar)
    5. Check that run_game with wait_for_bridge actually waits for bridge connection
    6. Check that node_path="" tools operate on scene root without error
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. UAT test file exists at tests/uat_phase16.py
2. All tests pass when run against live Godot editor
3. DX-01: run_game wait_for_bridge=true returns bridge_connected=true
4. DX-02: node_path="" and "." accepted by all tools (no "missing parameter" error)
</verification>

<success_criteria>
- All UAT tests pass (13/13 or similar count)
- Both DX-01 and DX-02 requirements validated end-to-end
- Human verification confirms correct behavior
</success_criteria>

<output>
After completion, create `.planning/phases/16-game-bridge-auto-wait/16-02-SUMMARY.md`
</output>
