---
phase: 13-runtime-state-query
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - tests/uat_phase13.py
autonomous: false
requirements: [RTST-01, RTST-02, RTST-03]

must_haves:
  truths:
    - "UAT script connects to MCP server and verifies 43 tools including 3 new runtime state query tools"
    - "get_game_node_property reads a known property from a running game node and returns value + type"
    - "get_game_node_property returns error for non-existent node"
    - "get_game_node_property returns error for non-existent property"
    - "eval_in_game evaluates a simple expression and returns the result"
    - "eval_in_game returns error for invalid expression syntax"
    - "get_game_scene_tree returns tree structure with name, type, path fields"
    - "get_game_scene_tree with max_depth=0 returns root only (no children)"
    - "Build compiles successfully with scons"
  artifacts:
    - path: "tests/uat_phase13.py"
      provides: "End-to-end UAT suite for Phase 13 runtime state query tools"
      min_lines: 400
  key_links:
    - from: "tests/uat_phase13.py"
      to: "src/mcp_server.cpp"
      via: "TCP JSON-RPC tool calls to port 6800"
      pattern: "call_tool.*get_game_node_property"
---

<objective>
Create UAT test suite for Phase 13 runtime state query tools and verify end-to-end functionality.

Purpose: Validate that all three RTST requirements work end-to-end through the complete C++ -> GDScript -> C++ pipeline.
Output: tests/uat_phase13.py with automated test cases, build verification, and human checkpoint.
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-runtime-state-query/13-CONTEXT.md
@.planning/phases/13-runtime-state-query/13-01-SUMMARY.md
@.planning/phases/12-input-injection-enhancement/12-02-SUMMARY.md

<interfaces>
<!-- Phase 13 tools API from Plan 01 -->

get_game_node_property:
  Required: node_path (string), property (string)
  Success response: {"value": "<var_to_str output>", "type": "<type_name>"}
  Error responses: "Node not found: <path>", "Property not found: <prop> on node: <path>"

eval_in_game:
  Required: expression (string)
  Success response: {"result": "<var_to_str output>"}
  Error responses: "Parse error: <text>", "Execute error: <text>"

get_game_scene_tree:
  Optional: max_depth (integer, default -1 = unlimited)
  Success response: {"name": "...", "type": "...", "path": "...", "children": [...]}
  Error responses: "No current scene"

All three are deferred response tools (15s timeout needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UAT test script for Phase 13</name>
  <files>tests/uat_phase13.py</files>
  <read_first>
    - tests/uat_phase12.py
    - src/mcp_tool_registry.cpp
  </read_first>
  <action>
Create `tests/uat_phase13.py` following the EXACT structure of `tests/uat_phase12.py`:

1. Same docstring header pattern listing prerequisites, requirements covered (RTST-01, RTST-02, RTST-03), and test descriptions.

2. Same `MCPClient` class with `connect()`, `close()`, `_send()`, `_recv()`, `call()`, `call_tool()`, `call_tool_text()` methods. Copy from uat_phase12.py verbatim.

3. Same `run_tests()` function structure with results list, pass/fail counting, and summary output.

4. Test cases (approximately 15 tests):

**Tool list verification:**
- `test_tool_count`: tools/list returns 43 tools (was 40)
- `test_tools_include_runtime_state_query`: verify get_game_node_property, eval_in_game, get_game_scene_tree in tool names

**Game lifecycle:**
- `test_start_game`: run_game mode=main, wait 3s for bridge
- `test_bridge_connected`: get_game_bridge_status shows connected=true
  - If bridge NOT connected: print early exit summary and sys.exit(1) (same pattern as phase 12)

**get_game_scene_tree (RTST-03):**
- `test_get_game_scene_tree`: call with no args (default depth), verify result has "name", "type", "path" fields, 15s timeout
- `test_get_game_scene_tree_depth_0`: call with max_depth=0, verify result has "name" and "type" but no "children" key (or empty children)

**get_game_node_property (RTST-01):**
- `test_get_node_property_position`: call with node_path="" (scene root), property="position", verify result has "value" and "type" keys, 15s timeout
- `test_get_node_property_visible`: call with node_path="", property="visible", verify result has "value" containing "true" and "type" containing "bool"
- `test_get_node_property_not_found_node`: call with node_path="NonExistentNode12345", property="position", verify result contains "error" with "not found"
- `test_get_node_property_not_found_property`: call with node_path="", property="nonexistent_property_xyz", verify result contains "error" with "not found"

**eval_in_game (RTST-02):**
- `test_eval_simple_math`: call with expression="2 + 2", verify result has "result" key containing "4"
- `test_eval_get_children_size`: call with expression="get_children().size()", verify result has "result" key containing a number >= 0
- `test_eval_string_concat`: call with expression='"hello" + " " + "world"', verify result has "result" key containing "hello world"
- `test_eval_parse_error`: call with expression="invalid syntax {{{{", verify result contains "error"

**Game stop:**
- `test_stop_game`: stop_game succeeds

Each test uses `call_tool_text(name, args, timeout=15)` for the deferred tools (same timeout pattern as Phase 12).

5. Same summary output format at the end:
```
=== PHASE 13 UAT SUMMARY ===
Passed: X/Y
Failed: Z
```

6. Same `if __name__ == "__main__"` with sys.exit(0 if all pass else 1).
  </action>
  <verify>
    <automated>cd D:/Workspace/Godot/godot-mcp-meow && python -c "import ast; ast.parse(open('tests/uat_phase13.py').read()); print('Syntax OK')" && grep -c "def test_" tests/uat_phase13.py && grep "get_game_node_property\|eval_in_game\|get_game_scene_tree" tests/uat_phase13.py | head -10</automated>
  </verify>
  <acceptance_criteria>
    - python syntax check passes (ast.parse succeeds)
    - grep -c "def test_" returns at least 12 (test functions)
    - grep "get_game_node_property" returns at least 3 matches (tool name in tests)
    - grep "eval_in_game" returns at least 3 matches (tool name in tests)
    - grep "get_game_scene_tree" returns at least 2 matches (tool name in tests)
    - grep "RTST-01\|RTST-02\|RTST-03" returns matches in docstring
    - grep "timeout=15" returns at least 5 matches (deferred tool calls)
    - grep "43" returns at least 1 match (tool count assertion)
    - grep "early exit\|EARLY EXIT" returns at least 1 match (bridge failure handling)
    - File has at least 400 lines
  </acceptance_criteria>
  <done>tests/uat_phase13.py exists with 12+ test cases covering all 3 RTST requirements, valid Python syntax, 15s timeouts for deferred tools, early exit on bridge failure, and structured summary output</done>
</task>

<task type="auto">
  <name>Task 2: Build verification</name>
  <files></files>
  <read_first>
    - src/game_bridge.h
    - src/game_bridge.cpp
  </read_first>
  <action>
Run the SCons build to verify all C++ changes compile:
```bash
cd D:/Workspace/Godot/godot-mcp-meow && scons platform=windows target=editor -j8
```

If build fails, diagnose and fix the compilation errors. Common issues:
- Missing include for nlohmann::json::parse (already included via game_bridge.h)
- String type mismatches in EngineDebugger message data
- Missing method implementations matching header declarations

After build succeeds, verify the GDScript syntax by checking for common errors:
- Indentation consistency (tabs, not spaces)
- Match statement syntax
- Function signatures
  </action>
  <verify>
    <automated>cd D:/Workspace/Godot/godot-mcp-meow && scons platform=windows target=editor -j8 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - scons build exits with success (no error return code)
    - No compilation errors in game_bridge.cpp, mcp_server.cpp, or mcp_tool_registry.cpp
    - Output DLL/shared library is produced in the expected location
  </acceptance_criteria>
  <done>Project compiles successfully with all Phase 13 changes, no warnings or errors in modified files</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification -- run UAT against live Godot editor</name>
  <files></files>
  <action>
Phase 13 runtime state query tools: get_game_node_property, eval_in_game, get_game_scene_tree. All implemented in C++ game bridge + GDScript companion with deferred response pattern. UAT test suite created. Build verified.

Steps for human:
1. Restart Godot editor to load the new DLL
2. Ensure the MCP Meow plugin is enabled
3. Run: python tests/uat_phase13.py
4. Verify all tests pass (check summary output)
5. Expected: 43 tools total, all runtime state query tests green
  </action>
  <verify>Human confirms all UAT tests pass</verify>
  <done>All Phase 13 UAT tests pass, human approved</done>
  <resume-signal>Type "approved" if all UAT tests pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Full verification:
1. `scons platform=windows target=editor -j8` compiles without errors
2. `python tests/uat_phase13.py` shows all tests passing
3. Tool count is 43 (was 40 after Phase 12)
4. Deferred tools respond within 15s timeout
</verification>

<success_criteria>
- UAT script exists with 12+ test cases covering RTST-01, RTST-02, RTST-03
- Build compiles successfully
- All UAT tests pass when run against live Godot editor
- get_game_node_property reads position, visible from scene root
- eval_in_game evaluates "2 + 2" and returns "4"
- get_game_scene_tree returns tree with name/type/path fields
- Error cases handled: non-existent node, non-existent property, parse error, depth limit
</success_criteria>

<output>
After completion, create `.planning/phases/13-runtime-state-query/13-02-SUMMARY.md`
</output>
