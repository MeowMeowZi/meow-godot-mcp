---
phase: 08-animation-system
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - tests/uat_phase8.py
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03, ANIM-04, ANIM-05]

must_haves:
  truths:
    - "UAT script can create an AnimationPlayer with a named Animation and verify it exists"
    - "UAT script can add all 4 track types and verify via get_animation_info"
    - "UAT script can insert, update, and remove keyframes and verify via get_animation_info"
    - "UAT script can query full animation info depth (list, tracks, keyframes)"
    - "UAT script can set duration, loop mode, step and verify via get_animation_info"
  artifacts:
    - path: "tests/uat_phase8.py"
      provides: "End-to-end UAT tests for all 5 ANIM requirements"
      contains: "def test_anim"
  key_links:
    - from: "tests/uat_phase8.py"
      to: "src/mcp_server.cpp"
      via: "TCP JSON-RPC calls to tools/call"
      pattern: "call_tool.*create_animation"
---

<objective>
Create Phase 8 UAT test script (tests/uat_phase8.py) covering all 5 ANIM requirements with end-to-end TCP JSON-RPC tests against a running Godot editor. Following the exact uat_phase7.py pattern.

Purpose: Prove all animation tools work correctly end-to-end through the MCP protocol pipeline (AI client -> bridge -> GDExtension -> Godot API).
Output: tests/uat_phase8.py with ~15 tests covering ANIM-01 through ANIM-05 plus error cases.
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-animation-system/08-CONTEXT.md
@.planning/phases/08-animation-system/08-RESEARCH.md
@.planning/phases/08-animation-system/08-01-SUMMARY.md
@.planning/phases/08-animation-system/08-02-SUMMARY.md

<interfaces>
<!-- UAT pattern from uat_phase7.py -->
From tests/uat_phase7.py:
```python
class MCPClient:
    def __init__(self, host="127.0.0.1", port=6800, timeout=10):
    def connect(self):
    def close(self):
    def send(self, method, params=None):
    def call_tool(self, tool_name, arguments=None):

def report(label, success, detail=""):
    # Prints PASS/FAIL with label and detail, increments global counters

def main():
    # Connect, setup scene, run tests, print summary, exit with code
```

<!-- Tool call pattern -->
```python
result = c.call_tool("create_animation", {"animation_name": "idle"})
# result is parsed JSON response
# Check result["content"][0]["text"] for the tool output JSON
```

<!-- Scene setup pattern -->
```python
# Create a fresh scene for testing
c.call_tool("create_scene", {"root_type": "Node2D", "scene_name": "test_anim_uat"})
# Create a child node to animate
c.call_tool("create_node", {"type": "Sprite2D", "name": "TestSprite"})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/uat_phase8.py with end-to-end animation system tests</name>
  <files>tests/uat_phase8.py</files>
  <read_first>
    - tests/uat_phase7.py (full file: MCPClient class, report function, test pattern, main structure)
    - .planning/phases/08-animation-system/08-CONTEXT.md (tool parameters and expected behavior)
    - .planning/phases/08-animation-system/08-RESEARCH.md (expected JSON response structures)
  </read_first>
  <action>
Create tests/uat_phase8.py following the exact structure of uat_phase7.py. The script connects via TCP to port 6800, sends JSON-RPC tool calls, and validates responses.

**Header docstring:**
```
Phase 8 UAT - Automated end-to-end tests for animation system tools.

Prerequisites:
  1. Open Godot project at project/ with the MCP Meow plugin enabled
  2. Ensure the MCP TCP server is listening on port 6800
  3. Run: python tests/uat_phase8.py

Tests cover:
  - tools/list shows 34 tools including all Phase 8 animation tools (ANIM-01..05)
  - create_animation creates AnimationPlayer + animation (ANIM-01)
  - create_animation on existing player adds new animation (ANIM-01)
  - create_animation duplicate name returns error (ANIM-01)
  - add_animation_track adds value track (ANIM-02)
  - add_animation_track adds position_3d track (ANIM-02)
  - add_animation_track with invalid type returns error (ANIM-02)
  - set_keyframe inserts keyframe on value track (ANIM-03)
  - set_keyframe inserts keyframe on position_3d track (ANIM-03)
  - set_keyframe updates existing keyframe (ANIM-03)
  - set_keyframe removes keyframe (ANIM-03)
  - get_animation_info returns full structure (ANIM-04)
  - get_animation_info specific animation query (ANIM-04)
  - set_animation_properties sets duration, loop, step (ANIM-05)
  - Round-trip: set_animation_properties then get_animation_info confirms values (ANIM-05)
```

**Copy MCPClient class and report() function exactly from uat_phase7.py.**

**Scene setup (in main()):**
1. `c.call_tool("create_scene", {"root_type": "Node2D", "scene_name": "test_anim_uat"})` -- create fresh test scene
2. `c.call_tool("create_node", {"type": "Sprite2D", "name": "TestSprite"})` -- create a node to animate

**Test 1: tools/list shows 34 tools**
- `result = c.send("tools/list")`
- Parse tool names from result
- Check total count is 34
- Check "create_animation", "add_animation_track", "set_keyframe", "get_animation_info", "set_animation_properties" are all present

**Test 2: create_animation creates new player + animation (ANIM-01)**
- `result = c.call_tool("create_animation", {"animation_name": "idle"})`
- Parse text response, check `"success": true`
- Check response contains "player_path" and "animation_name"

**Test 3: create_animation on existing player adds animation (ANIM-01)**
- Use player_path from Test 2
- `result = c.call_tool("create_animation", {"animation_name": "walk", "player_path": player_path})`
- Check success, verify animation_name is "walk"

**Test 4: create_animation duplicate returns error (ANIM-01)**
- `result = c.call_tool("create_animation", {"animation_name": "idle", "player_path": player_path})`
- Check response contains "error" and mentions "already exists"

**Test 5: add_animation_track adds value track (ANIM-02)**
- `result = c.call_tool("add_animation_track", {"player_path": player_path, "animation_name": "idle", "track_type": "value", "track_path": "TestSprite:modulate"})`
- Check success, verify track_index == 0, track_type == "value"

**Test 6: add_animation_track adds position_3d track (ANIM-02)**
- `result = c.call_tool("add_animation_track", {"player_path": player_path, "animation_name": "idle", "track_type": "position_3d", "track_path": "TestSprite"})`
- Check success, verify track_index == 1

**Test 7: add_animation_track invalid type returns error (ANIM-02)**
- `result = c.call_tool("add_animation_track", {"player_path": player_path, "animation_name": "idle", "track_type": "invalid_type", "track_path": "TestSprite"})`
- Check response contains "error"

**Test 8: set_keyframe inserts on value track (ANIM-03)**
- `result = c.call_tool("set_keyframe", {"player_path": player_path, "animation_name": "idle", "track_index": 0, "time": 0.0, "action": "insert", "value": "Color(1, 0, 0, 1)"})`
- Check success, action == "insert"

**Test 9: set_keyframe inserts on position_3d track (ANIM-03)**
- `result = c.call_tool("set_keyframe", {"player_path": player_path, "animation_name": "idle", "track_index": 1, "time": 0.0, "action": "insert", "value": "Vector3(0, 0, 0)"})`
- Check success
- Insert second keyframe: `{"track_index": 1, "time": 1.0, "action": "insert", "value": "Vector3(100, 0, 0)"}`

**Test 10: set_keyframe updates existing keyframe (ANIM-03)**
- `result = c.call_tool("set_keyframe", {"player_path": player_path, "animation_name": "idle", "track_index": 1, "time": 0.0, "action": "update", "value": "Vector3(10, 10, 0)"})`
- Check success, action == "update"

**Test 11: set_keyframe removes keyframe (ANIM-03)**
- `result = c.call_tool("set_keyframe", {"player_path": player_path, "animation_name": "idle", "track_index": 1, "time": 1.0, "action": "remove"})`
- Check success, action == "remove"

**Test 12: get_animation_info full query (ANIM-04)**
- `result = c.call_tool("get_animation_info", {"player_path": player_path})`
- Parse response, check "animations" array
- Verify "idle" and "walk" are both present in animation names
- For "idle": verify track_count >= 2, tracks have correct types and paths

**Test 13: get_animation_info specific animation (ANIM-04)**
- `result = c.call_tool("get_animation_info", {"player_path": player_path, "animation_name": "idle"})`
- Verify only "idle" animation returned
- Verify keyframe data present on tracks (time, value, transition fields)

**Test 14: set_animation_properties (ANIM-05)**
- `result = c.call_tool("set_animation_properties", {"player_path": player_path, "animation_name": "idle", "length": 2.5, "loop_mode": "linear", "step": 0.05})`
- Check success

**Test 15: Round-trip set_animation_properties then get_animation_info (ANIM-05)**
- `result = c.call_tool("get_animation_info", {"player_path": player_path, "animation_name": "idle"})`
- Parse the "idle" animation entry
- Verify length == 2.5 (or close to it, float comparison)
- Verify loop_mode == "linear"
- Verify step == 0.05 (or close)

**main() summary:**
```python
if __name__ == "__main__":
    main()
```
Print summary: "Phase 8 Animation System UAT: X/Y passed". Exit with code 0 if all pass, 1 otherwise.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/uat_phase8.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/uat_phase8.py exists
    - grep "class MCPClient" tests/uat_phase8.py returns match
    - grep "def report" tests/uat_phase8.py returns match
    - grep "create_animation" tests/uat_phase8.py returns matches (tool calls)
    - grep "add_animation_track" tests/uat_phase8.py returns matches
    - grep "set_keyframe" tests/uat_phase8.py returns matches
    - grep "get_animation_info" tests/uat_phase8.py returns matches
    - grep "set_animation_properties" tests/uat_phase8.py returns matches
    - grep "ANIM-01" tests/uat_phase8.py returns match (requirement tracing in comments)
    - grep "ANIM-02" tests/uat_phase8.py returns match
    - grep "ANIM-03" tests/uat_phase8.py returns match
    - grep "ANIM-04" tests/uat_phase8.py returns match
    - grep "ANIM-05" tests/uat_phase8.py returns match
    - grep "34 tools" tests/uat_phase8.py or grep "34" tests/uat_phase8.py returns match (tool count check)
    - python syntax check passes (no SyntaxError)
    - grep -c "def test_" tests/uat_phase8.py or grep -c "report(" tests/uat_phase8.py shows >= 15 test points
  </acceptance_criteria>
  <done>tests/uat_phase8.py has 15 end-to-end tests covering all 5 ANIM requirements with round-trip validation and error cases, following uat_phase7.py pattern exactly</done>
</task>

</tasks>

<verification>
- `python -c "import ast; ast.parse(open('tests/uat_phase8.py').read())"` -- valid Python syntax
- Script follows identical structure to tests/uat_phase7.py
- All 5 ANIM requirement IDs referenced in test comments
- 15 tests cover: creation, track addition, keyframe CRUD, querying, property setting, round-trip validation, error cases
</verification>

<success_criteria>
- tests/uat_phase8.py exists with valid Python syntax
- 15 tests covering all 5 ANIM requirements
- Round-trip validation: mutation tools set values, get_animation_info confirms them
- Error handling coverage: duplicate animation name, invalid track type
- Script ready to run against live Godot editor with `python tests/uat_phase8.py`
</success_criteria>

<output>
After completion, create `.planning/phases/08-animation-system/08-03-SUMMARY.md`
</output>
