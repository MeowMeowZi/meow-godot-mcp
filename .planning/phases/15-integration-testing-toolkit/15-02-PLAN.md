---
phase: 15-integration-testing-toolkit
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - tests/uat_phase15.py
autonomous: false
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "run_test_sequence appears in tools/list with correct schema (44 tools total)"
    - "test_game_ui appears in prompts/list (7 prompts total)"
    - "run_test_sequence executes click_node steps and returns pass/fail per step"
    - "run_test_sequence assertion with equals/contains/not_empty operators works"
    - "run_test_sequence with click_node + get_game_node_property produces UI automation assertion results"
    - "test_game_ui prompt returns a step-by-step workflow with tool references"
  artifacts:
    - path: "tests/uat_phase15.py"
      provides: "End-to-end test suite for Phase 15 Integration Testing Toolkit"
      min_lines: 200
  key_links:
    - from: "tests/uat_phase15.py"
      to: "src/mcp_server.cpp"
      via: "TCP JSON-RPC calls to tools/call run_test_sequence"
      pattern: "run_test_sequence"
    - from: "tests/uat_phase15.py"
      to: "src/mcp_prompts.cpp"
      via: "TCP JSON-RPC calls to prompts/get test_game_ui"
      pattern: "test_game_ui"
---

<objective>
Create the UAT test suite for Phase 15 Integration Testing Toolkit.

Purpose: Verify all 3 TEST requirements end-to-end: run_test_sequence batch execution (TEST-01), UI automation assertions via click_node + get_game_node_property (TEST-02), and test_game_ui prompt template (TEST-03). Human verification checkpoint confirms everything works against a live Godot instance.

Output: uat_phase15.py test script that validates all Phase 15 functionality.
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-integration-testing-toolkit/15-01-SUMMARY.md

Source files to reference:
@tests/uat_phase14.py
@src/mcp_tool_registry.cpp
@src/mcp_prompts.cpp

<interfaces>
<!-- The run_test_sequence tool schema (from Plan 01): -->

```json
{
  "name": "run_test_sequence",
  "inputSchema": {
    "type": "object",
    "properties": {
      "steps": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "action": { "type": "string", "enum": ["click_node", "get_game_node_property", "inject_input", "eval_in_game", "get_game_output", "get_node_rect", "get_game_scene_tree", "wait"] },
            "args": { "type": "object" },
            "assert": { "type": "object" },
            "description": { "type": "string" }
          },
          "required": ["action"]
        }
      }
    },
    "required": ["steps"]
  }
}
```

<!-- run_test_sequence result format: -->
```json
{
  "success": true,
  "total_steps": 3,
  "passed": 2,
  "failed": 1,
  "all_passed": false,
  "steps": [
    {
      "step": 0,
      "action": "click_node",
      "description": "Click button",
      "result": { "success": true, "clicked_position": { "x": 100, "y": 50 } },
      "passed": true
    },
    {
      "step": 1,
      "action": "get_game_node_property",
      "description": "Check label text",
      "result": { "value": "\"Hello\"", "type": "String" },
      "assertion": { "passed": true, "operator": "contains", "expected": "Hello", "actual": "\"Hello\"" },
      "passed": true
    }
  ]
}
```

<!-- test_game_ui prompt: available via prompts/get with name="test_game_ui" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UAT test suite for Phase 15</name>
  <read_first>
    - tests/uat_phase14.py (full file -- UAT pattern: MCPClient class, report/report_skip helpers, call_tool/call_tool_text, wait_for_bridge, test structure, summary formatting)
    - src/mcp_tool_registry.cpp (tool count -- currently 43, will be 44 after Plan 01 adds run_test_sequence)
    - src/mcp_prompts.cpp (prompt count -- currently 6, will be 7 after Plan 01 adds test_game_ui)
    - .planning/phases/15-integration-testing-toolkit/15-01-SUMMARY.md (what was implemented in Plan 01)
  </read_first>
  <files>tests/uat_phase15.py</files>
  <action>
Create `tests/uat_phase15.py` following the exact same structure as `tests/uat_phase14.py`. Reuse the MCPClient class, report/report_skip helpers, call_tool/call_tool_text, and wait_for_bridge verbatim.

**Test plan (15 tests):**

**Test 1: tools/list shows 44 tools including run_test_sequence (TEST-01)**
- Call tools/list, verify 44 tools total (was 43 in Phase 14, +1 run_test_sequence)
- Verify "run_test_sequence" is in the tool name list

**Test 2: run_test_sequence schema has steps array property (TEST-01)**
- From tools/list response, find the run_test_sequence tool
- Verify inputSchema.properties.steps exists and is type "array"
- Verify inputSchema.required contains "steps"

**Test 3: prompts/list shows 7 prompts including test_game_ui (TEST-03)**
- Call prompts/list
- Verify 7 prompts total (was 6 in Phase 11, +1 test_game_ui)
- Verify "test_game_ui" is in the prompt name list

**Test 4: prompts/get test_game_ui returns workflow text (TEST-03)**
- Call prompts/get with name="test_game_ui", arguments={"test_type": "button_click"}
- Verify response contains messages array with role="user" and content containing "click_node" and "run_test_sequence"

**Test 5: run_game with mode=main succeeds, bridge connects**
- Call run_game with mode=main
- Poll get_game_bridge_status until connected=true (max 10s)
- EARLY EXIT if bridge fails to connect (skip remaining tests)

**Test 6: run_test_sequence with single eval_in_game step (TEST-01)**
- Call run_test_sequence with steps=[{action: "eval_in_game", args: {expression: "2 + 2"}, assert: {property: "result", contains: "4"}, description: "Simple eval"}]
- Verify result has success=true, total_steps=1, passed=1, all_passed=true
- Verify steps[0].passed=true

**Test 7: run_test_sequence with get_game_scene_tree step (TEST-01)**
- Call run_test_sequence with steps=[{action: "get_game_scene_tree", args: {max_depth: 1}, description: "Get scene tree"}]
- Verify result has success=true, total_steps=1, steps[0].passed=true (no assertion, pass if no error)

**Test 8: run_test_sequence with inject_input step (synchronous) (TEST-01)**
- Call run_test_sequence with steps=[{action: "inject_input", args: {type: "key", keycode: "space"}, description: "Press space key"}]
- Verify result has success=true, total_steps=1, steps[0].passed=true
- Verify steps[0].result.success=true

**Test 9: run_test_sequence with get_game_output step (synchronous) (TEST-01)**
- First eval_in_game to print a marker: eval_in_game with expression 'print("UAT15_MARKER")'
- Wait 1 second
- Call run_test_sequence with steps=[{action: "get_game_output", args: {keyword: "UAT15_MARKER", clear_after_read: false}, assert: {property: "success", equals: "true"}, description: "Check game output"}]
- Verify result has success=true, steps[0].passed=true

**Test 10: run_test_sequence with click_node step (deferred) (TEST-01, TEST-02)**
- First get_game_scene_tree to find a valid Control node path
- Call run_test_sequence with steps=[{action: "click_node", args: {node_path: FOUND_NODE_PATH}, description: "Click a UI node"}]
- Verify result has success=true, steps[0] contains a result (success or error)
- If no Control node found, use eval_in_game instead and skip this test

**Test 11: run_test_sequence with get_game_node_property + assertion (TEST-01, TEST-02)**
- First get_game_scene_tree to find scene root name
- Call run_test_sequence with steps=[{action: "get_game_node_property", args: {node_path: "", property: "name"}, assert: {property: "value", not_empty: true}, description: "Get root node name"}]
- Verify result has success=true, steps[0].passed=true, steps[0].assertion.passed=true

**Test 12: run_test_sequence with failing assertion (TEST-01)**
- Call run_test_sequence with steps=[{action: "eval_in_game", args: {expression: "2 + 2"}, assert: {property: "result", equals: "999"}, description: "This should fail"}]
- Verify result has success=true, total_steps=1, passed=0, failed=1, all_passed=false
- Verify steps[0].passed=false, steps[0].assertion.passed=false

**Test 13: run_test_sequence multi-step sequence (TEST-01, TEST-02)**
- Call run_test_sequence with steps=[
    {action: "eval_in_game", args: {expression: "get_tree().current_scene.name"}, assert: {property: "result", not_empty: true}, description: "Get scene name"},
    {action: "get_game_node_property", args: {node_path: "", property: "visible"}, description: "Check visibility"},
    {action: "get_game_output", args: {clear_after_read: false}, assert: {property: "success", equals: "true"}, description: "Read game output"}
  ]
- Verify result has success=true, total_steps=3
- Verify all 3 steps have results

**Test 14: run_test_sequence with wait step (TEST-01)**
- Call run_test_sequence with steps=[
    {action: "eval_in_game", args: {expression: 'print("BEFORE_WAIT")'}, description: "Print before"},
    {action: "wait", args: {duration_ms: 200}, description: "Wait 200ms"},
    {action: "get_game_output", args: {keyword: "BEFORE_WAIT"}, assert: {property: "success", equals: "true"}, description: "Check output after wait"}
  ]
- Verify result has success=true, total_steps=3
- Verify the wait step has a result containing waited_ms

**Test 15: stop_game succeeds**
- Call stop_game
- Wait 1 second
- Call get_game_bridge_status, verify connected=false

**Important implementation notes:**
- Use 15-second timeout for all run_test_sequence calls (deferred response)
- Early exit with summary if bridge fails to connect in Test 5
- For Test 10 (click_node), search the scene tree for any Control-derived node. If none found, use report_skip
- Header/footer match Phase 14 pattern exactly
- All tests wrapped in try/except with report(N, name, False, f"Error: {e}")
  </action>
  <verify>
    <automated>cd D:\Workspace\Godot\godot-mcp-meow && python -c "import ast; ast.parse(open('tests/uat_phase15.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/uat_phase15.py exists and is valid Python
    - grep -c "run_test_sequence" tests/uat_phase15.py returns >= 5
    - grep -c "test_game_ui" tests/uat_phase15.py returns >= 2
    - grep -c "TEST-01" tests/uat_phase15.py returns >= 1
    - grep -c "TEST-02" tests/uat_phase15.py returns >= 1
    - grep -c "TEST-03" tests/uat_phase15.py returns >= 1
    - grep -c "def run_tests" tests/uat_phase15.py returns 1
    - grep -c "def _print_summary" tests/uat_phase15.py returns 1
    - File contains 15 test cases (grep -c "report(" returns >= 15)
  </acceptance_criteria>
  <done>
    uat_phase15.py contains 15 tests covering all 3 TEST requirements. Script parses without syntax errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 15 end-to-end</name>
  <files>tests/uat_phase15.py</files>
  <action>
    Run the UAT suite against a live Godot editor and verify all Phase 15 functionality works correctly.
    What was built: run_test_sequence MCP tool (async state machine with deferred sub-step orchestration), test_game_ui prompt template (4 variants), and 15-test UAT suite.
    How to verify:
    1. Ensure Godot editor is open with the MCP Meow plugin enabled
    2. Ensure a main scene is configured (any scene with at least one Control node preferred)
    3. Run: python tests/uat_phase15.py
    4. Verify output shows 15/15 tests passing, tool count is 44, prompt count is 7
    5. Verify run_test_sequence returns structured pass/fail report with working assertions
    6. Verify in an MCP client (Claude/Cursor) that prompts/list shows "test_game_ui"
  </action>
  <verify>
    <automated>cd D:\Workspace\Godot\godot-mcp-meow && python tests/uat_phase15.py</automated>
  </verify>
  <done>
    Human confirmed: all 15 UAT tests pass, run_test_sequence orchestrates existing tools correctly, assertions produce accurate pass/fail results, test_game_ui prompt is accessible.
  </done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('tests/uat_phase15.py').read())"` succeeds (no syntax errors)
2. `python tests/uat_phase15.py` shows 15/15 tests passing
3. run_test_sequence tool appears in tools/list (44 tools total)
4. test_game_ui prompt appears in prompts/list (7 prompts total)
5. run_test_sequence with click_node + get_game_node_property produces structured assertion results
</verification>

<success_criteria>
- 15/15 UAT tests pass against a live Godot editor
- TEST-01 verified: run_test_sequence executes batch steps and returns structured report
- TEST-02 verified: click_node + get_game_node_property assertions work within run_test_sequence
- TEST-03 verified: test_game_ui prompt returns step-by-step workflow referencing MCP tools
- Human approved the results
</success_criteria>

<output>
After completion, create `.planning/phases/15-integration-testing-toolkit/15-02-SUMMARY.md`
</output>
