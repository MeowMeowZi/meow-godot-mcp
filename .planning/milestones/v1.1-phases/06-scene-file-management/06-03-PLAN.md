---
phase: 06-scene-file-management
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - tests/uat_phase6.py
autonomous: true
requirements:
  - SCNF-01
  - SCNF-02
  - SCNF-03
  - SCNF-04
  - SCNF-05
  - SCNF-06

must_haves:
  truths:
    - "UAT test script exercises all 6 SCNF requirements end-to-end through the MCP protocol"
    - "UAT tests verify save_scene, open_scene, list_open_scenes, create_scene, and instantiate_scene"
    - "UAT passes when run against a live Godot editor with the MCP plugin enabled"
  artifacts:
    - path: "tests/uat_phase6.py"
      provides: "End-to-end UAT test script for Phase 6 scene file management"
      contains: "Phase 6 UAT"
      min_lines: 200
  key_links:
    - from: "tests/uat_phase6.py"
      to: "src/mcp_server.cpp"
      via: "TCP JSON-RPC calls to tools/call for each scene file tool"
      pattern: "call_tool.*save_scene|open_scene|list_open_scenes|create_scene|instantiate_scene"
---

<objective>
Create the Phase 6 UAT test script that validates all 6 SCNF requirements end-to-end against a live Godot editor.

Purpose: Proves that all scene file management tools work correctly through the full MCP pipeline (AI client -> bridge -> GDExtension -> Godot API). This is the quality gate before the phase can be marked complete.

Output: tests/uat_phase6.py -- executable UAT script following the established MCPClient + report() pattern.
</objective>

<execution_context>
@C:/Users/28186/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/28186/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scene-file-management/06-CONTEXT.md
@.planning/phases/06-scene-file-management/06-RESEARCH.md
@.planning/phases/06-scene-file-management/06-02-SUMMARY.md

<interfaces>
<!-- UAT test script pattern from tests/uat_phase5.py -->

MCPClient class pattern:
```python
class MCPClient:
    def __init__(self, host="127.0.0.1", port=6800, timeout=10):
    def connect(self): ...
    def close(self): ...
    def request(self, method, params=None): ...  # Returns JSON-RPC response
    def notify(self, method, params=None): ...

def call_tool(client, tool_name, arguments=None):
    """Call a tool via tools/call and return parsed content JSON."""
    resp = client.request("tools/call", {"name": tool_name, "arguments": arguments or {}})
    result = resp.get("result", {})
    content_list = result.get("content", [])
    if content_list and "text" in content_list[0]:
        return json.loads(content_list[0]["text"]), resp
    return result, resp
```

Report pattern:
```python
PASS = "\033[92mPASS\033[0m"
FAIL = "\033[91mFAIL\033[0m"
results = []
def report(num, name, passed, detail=""): ...
```

Expected tool response format (from 06-CONTEXT.md):
- Success: {"success": true, "path": "res://..."} + operation-specific fields
- Failure: {"error": "description"}
- list_open_scenes: {"success": true, "scenes": [{path, title, is_active}], "count": N}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/uat_phase6.py with end-to-end tests for all 6 SCNF requirements</name>
  <files>tests/uat_phase6.py</files>
  <read_first>
    - tests/uat_phase5.py
    - .planning/phases/06-scene-file-management/06-CONTEXT.md
    - .planning/phases/06-scene-file-management/06-RESEARCH.md
  </read_first>
  <action>
Create tests/uat_phase6.py following the exact structure of tests/uat_phase5.py. Copy the MCPClient class, report() helper, and call_tool() helper verbatim from uat_phase5.py. Then implement these tests:

**Test 1: tools/list shows 23 tools including all Phase 6 tools**
- Call `client.request("tools/list")`
- Extract tool names from response
- Verify total count == 23
- Verify these 5 names are present: `save_scene, open_scene, list_open_scenes, create_scene, instantiate_scene`

**Test 2: create_scene creates a new scene with Node2D root (SCNF-04)**
- Call `call_tool(client, "create_scene", {"root_type": "Node2D", "path": "res://test_uat_scene.tscn", "root_name": "TestRoot"})`
- Verify `data["success"] == True`
- Verify `data["path"] == "res://test_uat_scene.tscn"`
- Verify `data["root_type"] == "Node2D"`
- Add `time.sleep(0.5)` after to let editor stabilize

**Test 3: list_open_scenes shows the created scene (SCNF-03)**
- Call `call_tool(client, "list_open_scenes")`
- Verify `data["success"] == True`
- Verify `data["count"]` is >= 1
- Verify at least one scene in `data["scenes"]` has `path` containing `test_uat_scene.tscn`
- Verify each scene entry has keys: `path`, `title`, `is_active`

**Test 4: save_scene saves current scene without path (overwrite) (SCNF-01)**
- Call `call_tool(client, "save_scene")` (no path -- overwrite mode)
- Verify `data["success"] == True`
- Verify `data["path"]` contains `test_uat_scene.tscn`

**Test 5: save_scene with path saves to new location (save-as / SCNF-05)**
- Call `call_tool(client, "save_scene", {"path": "res://test_uat_scene_copy.tscn"})`
- Verify `data["success"] == True`
- Verify `data["path"] == "res://test_uat_scene_copy.tscn"`

**Test 6: open_scene opens an existing scene file (SCNF-02)**
- Call `call_tool(client, "open_scene", {"path": "res://test_uat_scene_copy.tscn"})`
- Verify `data["success"] == True`
- Verify `data["path"] == "res://test_uat_scene_copy.tscn"`
- Add `time.sleep(0.5)` after

**Test 7: list_open_scenes shows both scenes after open_scene**
- Call `call_tool(client, "list_open_scenes")`
- Verify `data["count"]` >= 2
- Verify scenes include both `test_uat_scene.tscn` and `test_uat_scene_copy.tscn`

**Test 8: open_scene with non-existent file returns error**
- Call `call_tool(client, "open_scene", {"path": "res://nonexistent_scene.tscn"})`
- Verify `"error"` key exists in data
- Verify error message contains "not found" (case-insensitive)

**Test 9: create_scene with invalid class returns error**
- Call `call_tool(client, "create_scene", {"root_type": "FakeNodeClass", "path": "res://test_fake.tscn"})`
- Verify `"error"` key exists in data
- Verify error message contains "Unknown class" or "not a Node"

**Test 10: create_scene with non-Node class returns error**
- Call `call_tool(client, "create_scene", {"root_type": "Resource", "path": "res://test_resource.tscn"})`
- Verify `"error"` key exists in data
- Verify error message contains "not a Node"

**Test 11: instantiate_scene adds a sub-scene (SCNF-06)**
- First, switch back to test_uat_scene.tscn: `call_tool(client, "open_scene", {"path": "res://test_uat_scene.tscn"})`
- `time.sleep(0.5)`
- Call `call_tool(client, "instantiate_scene", {"scene_path": "res://test_uat_scene_copy.tscn", "name": "InstancedChild"})`
- Verify `data["success"] == True`
- Verify `data["path"]` contains "InstancedChild"
- Verify `data["scene_path"] == "res://test_uat_scene_copy.tscn"`

**Test 12: instantiate_scene with non-existent scene returns error**
- Call `call_tool(client, "instantiate_scene", {"scene_path": "res://nonexistent.tscn"})`
- Verify `"error"` key exists in data

**Test 13: get_scene_tree shows instantiated child (cross-validation)**
- Call `call_tool(client, "get_scene_tree")`
- Verify response contains a node named "InstancedChild" somewhere in the tree

**Cleanup section (after all tests):**
- Delete test files by calling `call_tool(client, "delete_node", {"node_path": "InstancedChild"})` (remove instantiated node)
- Note: test .tscn files (test_uat_scene.tscn, test_uat_scene_copy.tscn) left on disk; can be manually cleaned or ignored.

**Summary section:**
- Print pass/fail counts, exit 1 if any failures, exit 0 if all pass.
- Follow the exact summary format from uat_phase5.py.

The script header docstring should document:
```
Phase 6 UAT - Automated end-to-end tests for scene file management tools.

Prerequisites:
  1. Open Godot project at project/ with the MCP Meow plugin enabled
  2. Ensure the MCP TCP server is listening on port 6800
  3. Run: python tests/uat_phase6.py

Tests cover:
  - tools/list shows 23 tools including all Phase 6 tools
  - create_scene creates a new scene with specified root type
  - list_open_scenes returns open scene list with active detection
  - save_scene overwrites current file (SCNF-01)
  - save_scene with path saves to new location (SCNF-05)
  - open_scene opens existing scene file (SCNF-02)
  - open_scene with invalid path returns error
  - create_scene with invalid/non-Node class returns error
  - instantiate_scene adds sub-scene with undo/redo (SCNF-06)
  - instantiate_scene with non-existent scene returns error
  - Cross-validation: scene tree contains instantiated child
```
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('D:/Workspace/Godot/godot-mcp-meow/tests/uat_phase6.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/uat_phase6.py exists and is valid Python (no syntax errors)
    - tests/uat_phase6.py contains `class MCPClient` (TCP JSON-RPC client)
    - tests/uat_phase6.py contains `def call_tool` helper function
    - tests/uat_phase6.py contains `def report` helper function
    - tests/uat_phase6.py contains `call_tool(client, "save_scene"` (SCNF-01)
    - tests/uat_phase6.py contains `call_tool(client, "open_scene"` (SCNF-02)
    - tests/uat_phase6.py contains `call_tool(client, "list_open_scenes"` (SCNF-03)
    - tests/uat_phase6.py contains `call_tool(client, "create_scene"` (SCNF-04)
    - tests/uat_phase6.py contains `"res://test_uat_scene_copy.tscn"` (SCNF-05 save-as)
    - tests/uat_phase6.py contains `call_tool(client, "instantiate_scene"` (SCNF-06)
    - tests/uat_phase6.py contains `"Phase 6 UAT"` in output strings
    - tests/uat_phase6.py contains at least 13 `report(` calls (13 tests)
    - tests/uat_phase6.py ends with `sys.exit(0)` on success and `sys.exit(1)` on failure
  </acceptance_criteria>
  <done>UAT test script covers all 6 SCNF requirements with 13 end-to-end tests, valid Python, ready to run against live Godot editor.</done>
</task>

</tasks>

<verification>
1. Syntax check: `python -c "import ast; ast.parse(open('tests/uat_phase6.py').read())"` -- no errors.
2. Full UAT (requires Godot editor running with plugin): `python tests/uat_phase6.py` -- all 13 tests pass.
3. Unit test regression: `cd tests/build && ctest --output-on-failure -C Release` -- all existing tests still pass.
</verification>

<success_criteria>
- tests/uat_phase6.py exists with 13 tests covering all SCNF requirements
- Script is syntactically valid Python
- When run against a live Godot editor with the plugin: all 13 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-scene-file-management/06-03-SUMMARY.md`
</output>
